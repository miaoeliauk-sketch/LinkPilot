# VoicePilot（暂定名）· AI口播文案检测与改写工具 V0.1 开发任务书

> 项目名待最终确认，候选：VoicePilot / 口播体检 / 人话。文中先用 VoicePilot 占位，改名只需替换代码里的项目名即可。

> 命名说明：GitHub仓库及工程代号仍为 `LinkPilot`。本工具是与 InkPilot（国风手账素材工具）**完全无关的全新内容**，两者共用仓库但代码零耦合，互不 import。产品名定稿前不调整仓库名、远程地址及相关配置。

> 交给 Claude Code 前，先完成【第0步：文本生成通道与分词库选型】+【第0.5步：建校准语料集】，再进入正式开发。

---

## 第0步：文本生成通道与分词库选型（开工前必做）

### 文本通道

三个候选，各跑同一段测试稿一遍，输出对比表：

| 候选 | 说明 |
|---|---|
| A. Claude Code 会话内直接生成 | 零成本，但不能脚本化批量跑，做不了回归测试 |
| B. 直接调用 Claude API | 可脚本化、可批量、可控温度，需要单独的 API Key |
| C. 复用 InkPilot 已装的 Codex CLI 桥接 | 已有订阅，零新增成本，但要确认文本任务的调用方式和稳定性 |

对比表必须包含：**能否脚本化批量跑**（决定能不能做回归测试，权重最高）、单篇延迟、单篇成本、同一段稿子跑3次的判定稳定性。

具体模型在选型时确认，本文档不写死型号。**通道没定之前不要开始写 L2 检测层**，但 L1 规则层不依赖通道，Day 1 可以先写。

### 分词库

可视化名词密度、形容词/名词比这两项指标需要中文分词。候选：`nodejieba` / `segmentit` / 纯正则兜底。

**如果分词方案 Day 2 还没跑通，这两项指标降级为"人工目测"，不阻塞其余指标**——不要为了分词卡住整个项目。

---

## 第0.5步：建校准语料集（开工前必做）

准备两组稿子，各 20 篇，纯人工收集，**不靠AI生成**：

- **人稿组**：20 篇真人口播逐字稿（从真实视频扒下来的口语原文，不是公众号文章）
- **AI稿组**：20 篇用常见提示词让模型直接生成的口播稿

这是本项目的地基。**检测器判不准，后面全部白干**——所有阈值都要靠这 40 篇校准，不能拍脑袋定。

存放路径：`corpus/human/*.txt`、`corpus/ai/*.txt`，每篇附一行来源说明。

---

## 定位

一个**独立的命令行工具**，不合并进 InkPilot 代码库；共用仓库但目录独立（`src/voice/`），后续确认要拆独立仓库时整目录迁出即可。

输入一段已有的中文口播文案，输出三样东西：

1. **句子级标注**——哪几句有 AI 味，每句给出命中的具体规则和原因
2. **全文体检报告**——各项指标的实测值和阈值对比
3. **定向改写建议**——只针对被标记的句子给改写版，附改写理由

只处理文字，不配音、不剪辑、不生成新选题。

---

## 核心难点（先说清楚，这是本项目的"风格一致性"）

**误报比漏报伤得多。** 把人自己写的句子标成"AI写的"，用户会立刻不信任整个工具；漏掉一两句，用户顶多觉得不够灵敏。所以 V0.1 的阈值一律往保守调，**宁可漏报不可误报**。

**"AI味"不等于"AI写的"。** 本工具做的是**风格诊断**，不是司法级的 AI 生成检测：

- 人写的公文腔稿子，会被判为高 AI 味——这是对的，工具的目标是"听起来像不像人说话"
- 模型写完被人精修过的稿子，可能判为低 AI 味——这也是对的
- 本工具**不承诺、也不尝试**回答"这段是不是某个模型生成的"，学术界的 AI 文本检测本身就不可靠，不要对外宣称这个能力

把这条写进 README 和 CLI 的帮助文本里，避免用户误用。

---

## V0.1 范围（严格控制边界）

### ✅ 只做这些

1. 输入一段中文口播文案（`.txt` 文件或标准输入），输出检测报告
2. **L1 规则层检测**：7 项确定性指标，纯程序计算，零模型调用（见下）
3. **L2 模型判断层**：补规则抓不到的"平均说法"问题，返回句子级判断
4. 两层结果合并成句子级标注，标注分三档：`AI味` / `存疑` / `通过`
5. 输出全文体检报告（各指标实测值 vs 阈值，总分）
6. **定向改写**：只对被标记句子给改写版，附改写理由
7. 禁用词表、阈值配置全部走外部 JSON，加词改阈值不用改代码
8. 保存输入原文、各项指标值、完整提示词、模型原始返回、最终报告（供复现和调参）

### ❌ 明确不做（写进任务书是为了防止范围蔓延）

- **从选题/关键词生成整篇稿**（属于 V0.2；届时直接复用本工具当生成后的校验器，这是本项目先做检测的主要理由）
- 自动配音 / TTS
- 自动剪辑、配画面、B-roll 方案
- "是否由某个具体模型生成"的司法级判定（见上方"核心难点"）
- 整篇重写（只做定向改写，理由见"关键原则"）
- 播放量预估、爆款预测、热点选题抓取
- 账号人设微调 / LoRA / 语料训练
- 多语言（先只做中文）
- 网页界面、会员系统、团队协作、移动端
- 与 InkPilot 的任何功能打通

### ⚠️ 实验性支持（效果不保证，V0.1验收不强制要求达标）

- **口语化程度极高的稿子**（大量方言、口头禅、语气词）——句长和分词指标都会失真
- **带货/专业领域稿**（医美、金融、医疗）——这类稿子本身就大量使用模板句式，人写的也会高分误报
- **超过 3 分钟的长稿**——L2 层一次送入过长文本，句子级定位准确度会下降
- **已经被人精修过的 AI 稿**——改写痕迹会同时削弱 L1 和 L2 信号，这是本工具的原理性盲区

---

## 技术方案

### 整体流程

```
输入：一段中文口播文案（.txt 或 stdin）
  ↓
【程序处理】按句号/问号/感叹号/换行切句，保留原文偏移量（用于回标位置）
  ↓
【程序处理】L1 规则层：7项指标逐句 + 全文计算，纯确定性，零模型调用
  ↓
【文本生成通道】L2 模型判断层：整段送入，要求返回 JSON，
   每条判断必须引用原文片段（引用对不上则丢弃该条，防止模型瞎标）
  ↓
【程序处理】两层结果合并：
   L1 硬命中 → 直接标 `AI味`
   仅 L2 命中 → 只标 `存疑`，不标 `AI味`（保守策略，压误报）
   两层都未命中 → `通过`
  ↓
【程序处理】校验 L2 返回的引用能否在原文中找到，找不到的整条丢弃，
   丢弃比例超过 50% 则判定本次 L2 调用失败，**最多重试2次**，
   仍失败则只输出 L1 结果并标注"L2 未生效"
  ↓
【文本生成通道】对被标记句子逐条定向改写，改写提示词里带上该句命中的具体规则
  ↓
输出：体检报告 .json + 带标注的可读报告 .md
```

### 关键原则（V0.1必须遵守）

- **只改被标记的句子，不动没被标记的句子。** 整篇重写会把用户自己的表达一起洗掉，最后产出的还是"平均说法"，与工具目标直接矛盾。（同 InkPilot"装饰只绘制在透明区域，不修改主体像素"的原则。）
- **L1 规则层必须能独立运行。** 不联网、不调模型也要能出一份报告——这是工具的兜底能力，也是回归测试的基础。
- **L2 的每条判断必须引用原文。** 引用在原文里找不到就整条丢弃。模型很擅长给出听起来有道理但指不到具体位置的判断，这类判断对用户毫无价值。
- **不能让模型给自己打总分。** 总分由 L1 指标程序化合成，L2 只贡献句子级的"存疑"标记，不参与算分。问模型"这段有AI味吗"，它倾向于说没有。
- **所有阈值来自第0.5步的 40 篇校准集，不拍脑袋。** 每次调阈值都要重跑校准集，记录误报率/召回率的变化。
- **禁用词表是外部 JSON，不写死在代码里。** 加词是持续工作，加词不应该需要改代码和发版。

### L1 规则层：7项指标

| 指标 | 算法 | V0.1 初始阈值 | 备注 |
|---|---|---|---|
| 指纹词命中 | 禁用词表全表匹配，逐句 | 命中即标 | 最强信号，误报率最低 |
| 三连排比 | 正则检测"X的、X的、X的"类结构 | 出现即标 | |
| 平均句长 | 全文，按标点切句 | > 22 字告警 | 阈值靠校准集定 |
| 最长句 | 全文 | > 35 字告警 | |
| 逻辑连接词密度 | 首先/其次/再者/因此/综上/总而言之 | > 1 处/全文 | 真人靠停顿连接，不靠连词 |
| 可视化名词密度 | 分词后匹配实体名词表 | < 1 个/10秒 告警 | 分词没跑通则降级人工目测 |
| 字数-时长比 | 按 260 字/分钟 换算 | 偏离标称时长 ±15% | 用户声明目标时长时才启用 |

初始阈值全部是待校准值，Day 2 跑完 40 篇校准集后统一修订并记录在案。

### L2 模型判断层：只查规则查不到的三类

1. **无具体信息**——整段只有形容词没有实体（"质感细腻，体验极佳"，而不是"120克宣纸，笔一压就洇"）
2. **平均说法**——关于这个话题的中位数表达，换谁来说都一样，没有个人立场和亲身经历
3. **书面语伪装口语**——长定语从句、四字词堆砌，念出来会缺氧

输出格式固定为 JSON，每条包含：`quote`（原文片段，必填，用于回标）、`type`（上述三类之一）、`reason`（一句话）。

### 禁用词表 v0（`banned-phrases.json` 初始内容，后续持续加词）

```
开头类：你有没有发现 / 我敢打赌 / 说到XX不得不提 / 在这个XX的时代
句式类：不是XX而是XX / 与其XX不如XX / 真正的XX从来都不是
中段类：划重点 / 记住这一点 / 敲黑板 / 接下来这点很重要
形容词类：细腻 / 治愈 / 沉浸式 / 质感满满 / 独一无二 / 体验极佳
结尾类：你值得拥有 / 评论区聊聊 / 希望对你有帮助 / 让我们一起
```

---

## 数据留存要求（不能只存最终报告）

每次检测必须保存以下内容，供以后复现和优化：

```json
{
  "input_text": "原文完整内容",
  "input_source": "文件路径 / stdin",
  "declared_duration": "用户声明的目标时长，可为空",
  "l1_metrics": {
    "指纹词命中": ["命中的词及所在句"],
    "平均句长": 0,
    "最长句": 0,
    "逻辑连接词密度": 0,
    "可视化名词密度": 0,
    "字数时长偏差": 0
  },
  "l2_raw_response": "模型原始返回，未经处理",
  "l2_dropped_count": "因引用对不上被丢弃的条数",
  "sentence_labels": [{ "index": 0, "text": "", "label": "AI味/存疑/通过", "hits": [] }],
  "rewrites": [{ "index": 0, "original": "", "rewritten": "", "reason": "" }],
  "config_version": "禁用词表和阈值配置的版本号",
  "timestamp": "检测时间",
  "human_review": "准 / 误报 / 漏报（先留空字段，V0.1不做评分系统，但字段要预留）"
}
```

`config_version` 是必需字段：阈值会持续调整，没有版本号就无法解释历史报告为什么和现在结果不同。

---

## 验收标准（V0.1完成的判断标准）

- [ ] 输入一段口播稿，能跑完整流程，得到句子级标注 + 体检报告 + 定向改写建议
- [ ] **人稿误报率 ≤ 10%**（20 篇人稿中，被判为高 AI 味的不超过 2 篇）——这是最硬的一条，不通过则 V0.1 不算完成
- [ ] **AI稿召回率 ≥ 70%**（20 篇 AI 稿中，至少 14 篇被判为高 AI 味）
- [ ] 断网/不调模型的情况下，L1 层能独立跑出完整报告
- [ ] L2 返回的每条判断都能在原文中定位到，对不上的已被丢弃且计数
- [ ] 定向改写只改被标记句子，未标记句子逐字不变（程序化 diff 校验）
- [ ] 同一段稿子跑 3 次，L1 结果完全一致，L2 标记的句子重合度 ≥ 70%
- [ ] 所有检测记录都有保存，可以在文件夹里翻到原文+指标+模型返回+配置版本

---

## 建议的第一周任务拆分

| 天 | 任务 |
|---|---|
| Day 0 | 完成"第0步通道与分词库选型"+"第0.5步建 40 篇校准集"，输出通道对比表 |
| Day 1 | 写切句 + L1 规则层前 3 项（指纹词、三连排比、句长），跑通"输入txt→输出指标"最简闭环，不管准确度，先通 |
| Day 2 | L1 补齐 7 项，跑 40 篇校准集，**统一修订全部阈值并记录**；分词没跑通就降级那两项 |
| Day 3 | 接入 L2 模型判断层 + 引用回标校验 + 重试逻辑，跑校准集看误报率 |
| Day 4 | 两层结果合并策略 + 句子级标注输出，重跑校准集确认误报率 ≤ 10% |
| Day 5 | 定向改写 + diff 校验（未标记句子必须逐字不变） |
| Day 6 | 数据留存 + 可读报告 .md 输出 + 整体验收 |
| Day 7 | 缓冲日，处理前面卡住的问题 |

如果 Day 2 跑完校准集发现**人稿误报率压不下去**（人写的稿子大量被标 AI 味），先停下来专门调规则和阈值，不要往下接 L2——误报是这个工具的生死线，Day 7 的缓冲日也是为这个问题预留的。

如果 Day 3 发现 L2 大量返回对不上原文的引用，先收紧提示词和输出格式约束，实在不行 V0.1 可以**只交付 L1 层**，把 L2 推到 V0.2——L1 独立可用是刻意设计的兜底。
