# Design — InkPilot（国风手账素材工具）

> 放在项目根目录。Claude Code 调用 imagegen 技能生成图片前会读取本文件，作为风格统一的依据。
> 修改这份文件 = 修改整个工具的视觉风格，不用去改代码里的提示词。

## Concept

国风手绘知识卡片风格：工笔白描线稿 + 方格纸手账质感，克制、清晰、留白多，
不是浓艳的古风插画，更接近“手账本里认真画的一页笔记”。

## Palette（固定色板，仅从以下五色中取用）

- 朱红 `#C23531` — 强调色，用于重点标记、印章
- 黛蓝 `#2E4057` — 主线条色/标题色
- 月白 `#F5F0E8` — 背景底色
- 藤黄 `#E8B004` — 次强调色，用于高亮
- 赭石 `#A0522D` — 阴影/次要装饰色

最终主体和成品不使用五色之外的颜色，尤其避免高饱和度的荧光色、渐变色。用于抠图且随后会被删除的纯绿色或品红色色度键背景属于临时技术色，不参与色板验收；主体不得使用色度键颜色。

**提示词与验收标准的区别**：以上“仅从五色中取用”是对**提示词/生成指令中的主体和最终成品**的要求，写提示词时必须严格指定这五个色板色，不能放宽；临时色度键背景不属于最终成品。但 Day 3 测试证实，图像模型的实际输出色值与指定色值会有偏差（例如指定 `#A0522D`，实际输出约 `#C05E27`）。因此 **V0.1 阶段的结果验收**按“颜色类别正确 + 视觉近似”判断即可，不要求生成结果逐像素精确匹配色板 hex 值，也暂不做程序化色板量化后处理。完整测试证据见 `Day3-测试结论.md`。

### 色彩映射规则

- 只保留原图中主体的款式、结构和明暗关系，不保留原始色相
- 除人物肤色例外外，提示词必须要求原图颜色映射到五色色板；实际结果按颜色类别正确、视觉近似验收
- 提示词必须明确写出关键物体的目标颜色，不能只写“使用固定色板”
- 例如：军绿色外套必须改为赭石色或黛蓝色，禁止保留军绿色、橄榄绿色或相近色
- 人物肤色允许保留自然、低饱和的暖中性色，不强制映射到固定五色色板；除随后会被删除的临时色度键背景外，服装、物件、最终背景和装饰仍须严格映射

## Illustration Style

- 工笔白描线稿为主，线条粗细均匀，无厚重阴影渲染；发丝和衣褶允许少量疏朗的结构线，禁止密集平行排线、交叉影线、铅笔素描质感和雕版画式大面积阴影
- 主体保持原图的姿势、轮廓、手持物品、视角——只改变绘画技法，不改变构图
- 留白比例高，装饰元素不能挤占主体空间
- 背景生成时统一用可去除的纯绿色或品红色色度键底，主体不得使用该色（用于后续本地抠图，输出透明 PNG）
- 三档强度：
  - **轻度**：保留原图大部分写实感，仅做轻微线条化和色调统一
  - **标准**（默认）：明确的手绘线稿 + 国风配色，装饰元素适中
  - **浓郁**：强烈水墨/工笔感，装饰元素丰富

## Decorative Elements（程序化叠加，不由AI生成）

以下元素由固定PNG素材程序化贴图，不依赖每次AI生成，保证稳定：
- 印章（朱红色，方形/圆形）
- 云纹、竹叶（边角点缀，不遮挡主体）
- 胶带、便签（用于“手账”质感）
- 方格纸/宣纸纹理背景

## Typography（预留，V0.1不涉及）

- 标题：待选定毛笔/书法感字体
- 正文：延续现有图文账号使用的手写体
- 文字不由图像模型生成，由程序化排版引擎渲染

## Avoid（禁止出现的视觉特征）

- 欧美卡通风
- 日漫风（大眼睛、赛璐璐渲染）
- 过度写实/照片级质感
- 非中式的建筑、服饰、场景元素
- 高饱和度渐变色、荧光色（随后会被删除的临时色度键背景除外）
- 五色色板之外的颜色（自然、低饱和的暖中性人物肤色及临时色度键背景除外）
- 密集平行排线、交叉影线、铅笔素描质感、雕版画式大面积阴影

## Prompt 五段式模板（人工调用时的参考）

```
场景/背景：[纯绿色或品红色色度键背景，用于后续抠图；该背景是随后删除的临时技术色，不参与最终色板验收，主体不得使用该色]
主体：[描述需保留的姿势/轮廓/手持物品/视角，明确“仅改变绘画技法”；同时写明原图关键颜色到固定色板的具体映射，不保留原始色相]
细节：工笔白描线稿，标准浓郁度，线条粗细均匀，无大面积阴影渲染，留白比例高；发丝和衣褶允许少量疏朗结构线，禁止密集平行排线、交叉影线、铅笔素描质感和雕版画式大面积阴影
用途：[输出尺寸/用途，例如“知识卡片贴纸素材，正方形，1024×1024”；模型实际输出尺寸可能与请求不一致，生成后由程序统一缩放]
限制：除自然、低饱和的暖中性人物肤色和随后删除的临时色度键背景外，仅使用色板 #C23531/#2E4057/#F5F0E8/#E8B004/#A0522D；主体不得使用色度键颜色；禁止欧美卡通风、日漫风、写实照片质感、非中式元素
```
