# V0.2 Codex 任务卡集合

> 说明：以下任务卡供在本地机器上执行实际图像生成/测试的 Codex/Claude Code 会话使用。每张任务卡独立、可单独派发。所有任务卡共同遵守本文件顶部的全局规则，不在每张卡内重复展开。

## 全局规则（所有任务卡共同遵守）

1. **本地提交要求**：所有代码/文档改动在本地 git 仓库提交，提交信息需清楚描述改动内容。
2. **不得推送远程仓库**：任何任务卡都不得执行 `git push`。远程推送统一由用户在确认后手动执行。
3. **真实人物素材**：仅本地保存，不写入任何会被提交到公开仓库的路径（检查 `.gitignore` 或在提交前用 `git status` 确认）。
4. **不伪造实验结果**：没有真实运行产生的数据，一律留空或写"待实测"，不得编造数字、评分或结论性文字。
5. **调用次数与成本记录**：任何消耗 Codex/gpt-image-2 生成额度的任务卡必须在执行过程中实时记录调用次数，达到成本上限立即停止，不得自行扩大预算。
6. **接口兼容性**：涉及代码改动的任务卡必须遵守《V0.2-接口兼容性审查报告.md》列出的约束，改动后运行 `pnpm test`、`pnpm typecheck`确认V0.1既有回归用例未被破坏。

---

## 任务卡1：实验记录复核

- **背景**：首轮V0.2结构化提示词对照实验已执行完毕，摘要结论（36:31分、0比2硬失败、结构化提示词胜出等）尚未经过完整记录核对。
- **目标**：对照《阶段0-V0.2首轮实验记录核对清单.md》，逐项核实`V0.2-首轮对照实验记录.md`完整内容，填写核对结果。
- **前置条件**：`V0.2-首轮对照实验记录.md`完整文本可读取。
- **允许修改的文件**：`阶段0-V0.2首轮实验记录核对清单.md`（仅填写核对结果，不改动表格结构）。
- **禁止修改的文件**：`src/`目录下任何代码文件；`DESIGN.md`；`国风手账素材工具_V0.2开发任务书.md`（核对结果导致的任务书更新由本仓库维护者在收到核对结果后单独处理，不在本任务卡内直接改）。
- **是否允许生成图片**：否。
- **输入**：`V0.2-首轮对照实验记录.md`完整文本。
- **输出**：填写完毕的核对清单。
- **验收标准**：清单每一栏都有"可确认/存疑/无法确认"三选一的明确结论及依据，不允许空白或模糊表述。
- **成本上限**：0次生成调用（纯文档核对）。
- **停止条件**：完成全部核对项即停止；若记录本身缺失关键数据导致无法核对，如实标记"无法确认"并停止，不得推测填充。
- **本地提交要求**：核对完成后本地提交，提交信息注明"阶段0实验记录核对完成/部分完成"。
- **不得推送远程仓库**。

---

## 任务卡2：浓郁档统一复测

- **背景**：浓郁档白斑占比10.04%→1.82%的测量口径待确认，需要先按《V0.2-浓郁档测量规范.md》实现统一测量脚本，再复测。
- **目标**：实现测量脚本，先对Day6/Day7已有素材做基线复现测试，确认脚本口径与历史数据可比或记录差异；再对首轮V0.2实验的浓郁档素材（若可获取原始文件）复测。
- **前置条件**：任务卡1完成，且"浓郁档白斑占比"相关结论已有明确的"可确认/存疑/无法确认"标记；Day6/Day7原始素材文件可获取。
- **允许修改的文件**：新增测量脚本（建议路径 `scripts/measure-white-patches.ts`，与现有`scripts/generate-assets.ts`风格一致）；`阶段0-V0.2首轮实验记录核对清单.md`第六节。
- **禁止修改的文件**：`src/intensity.ts`（正式提示词逻辑，不在本任务卡范围）；`DESIGN.md`。
- **是否允许生成图片**：否（本任务卡只做已有素材的测量，不产生新的AI生成图片）。
- **输入**：Day6/Day7已有素材文件、（如可获取）首轮V0.2实验浓郁档素材文件。
- **输出**：测量脚本代码；基线复现测试结果；复测结果（JSON+人类可读摘要）。
- **验收标准**：
  - [ ] 基线复现测试有明确结论（口径一致或记录差异原因）
  - [ ] 复测结果不直接宣称"已优化"，除非基线复现测试确认口径一致
  - [ ] `pnpm test`、`pnpm typecheck`通过
- **成本上限**：0次生成调用。
- **停止条件**：基线复现测试失败（口径明显不可比）且无法获取足够历史素材重新计算时，如实记录"口径不可比，无法确认改善结论"并停止，不得强行给出数字。
- **本地提交要求**：本地提交，提交信息注明脚本用途和复测结论状态。
- **不得推送远程仓库**。

---

## 任务卡3：智能抠图原型验证

- **背景**：色度键对发丝、细叶类素材存在原理性局限，需要验证本地智能抠图是否能作为补充路线。
- **目标**：按《V0.2-智能抠图测试设计表.md》采集/使用已授权素材，运行色度键与智能抠图两条路线对照测试，填写测试表，给出转正/保留实验性/继续排除三选一决策。
- **前置条件**：至少3组发丝素材、3组细叶素材已采集且授权状态确认为"已授权"或"AI测试素材"（无需人物授权）。
- **允许修改的文件**：`V0.2-智能抠图测试设计表.md`（填写结果）；如需新增智能抠图运行脚本，放在独立路径（如`scripts/matting-prototype/`），不改动`src/`下现有pipeline代码。
- **禁止修改的文件**：`src/composite.ts`、`src/background.ts`、`src/intensity.ts`（本任务卡是独立原型验证，不接入正式pipeline）。
- **是否允许生成图片**：视具体测试设计而定——若智能抠图工具本身不需要AI生成新图（只是对已有照片做抠图处理），则不允许调用Codex生成新图，直接使用已有素材；若测试设计需要额外生成对照素材，需单独在任务卡执行前明确预算，默认不允许。
- **输入**：发丝、细叶素材（已授权或AI测试素材）。
- **输出**：填写完毕的测试表；抠图结果文件（本地保存，若含真人素材不进公开仓库）。
- **验收标准**：见《V0.2-智能抠图测试设计表.md》第六节"最终决策栏"，必须给出明确三选一决策及依据，不得留空或模糊表述为"效果不错"。
- **成本上限**：0次Codex图像生成调用（默认），如需调整需用户明确批准并在本任务卡执行前写明新的次数上限；若涉及生成调用，按《V0.2-成本与运行环境记录模板.md》记录，预算独立申请，不沿用其他任务卡额度。
- **停止条件**：完成全部测试表项或达到成本上限即停止。
- **本地提交要求**：本地提交，提交信息注明"智能抠图原型验证完成，决策：xxx"。
- **不得推送远程仓库**。

---

## 任务卡4：结构化提示词接入

- **背景**：首轮实验摘要显示结构化提示词优于现有五段式模板，但结论尚待核对。
- **目标**：**仅在前置条件满足时执行**——修改`src/intensity.ts`的`buildGenerationPlan`（或新增并行路径），接入结构化全字段提示词格式。
- **前置条件**：**任务卡1完成，且核对清单中"结构化提示词胜出"结论标记为"可确认"，并获得用户明确授权执行本任务卡。** 前置条件不满足时，本任务卡不得执行，直接停止并报告"前置条件未满足"。
- **允许修改的文件**：`src/intensity.ts`；`DESIGN.md`"Prompt 五段式模板"章节（如需补充结构化字段版本说明）；对应的`tests/intensity.test.ts`。
- **禁止修改的文件**：`src/metadata.ts`、`src/composite.ts`、`src/background.ts`、`src/types.ts`（除非结构化格式确实需要扩展类型定义，需在执行前单独说明原因）。
- **是否允许生成图片**：视具体验证需要，若需要生成对照样本验证改动效果，需在执行前由用户明确给出本次的次数上限——**不得援引首轮对照实验（16次预算）剩余的缓冲次数**，因为本任务卡是"把结论落地进正式代码"，不是首轮实验的延续，两者预算必须分开申请、分开记录。
- **输入**：核对通过的实验记录结论；现有`src/intensity.ts`代码。
- **输出**：修改后的`buildGenerationPlan`实现；更新后的测试。
- **验收标准**：
  - [ ] `GenerationPlan.prompt`保持字符串类型（见接口兼容性审查报告第六节约束2）
  - [ ] `pnpm test`、`pnpm typecheck`通过，V0.1既有9项回归用例未被破坏
  - [ ] DESIGN.md同步更新，不留旧文档与新代码不一致的情况
- **成本上限**：按前置条件批准时用户明确给出的次数上限执行，本任务卡不预设默认值，也不自动沿用其他任务卡/其他阶段的剩余预算；如涉及生成调用，按《V0.2-成本与运行环境记录模板.md》记录。
- **停止条件**：前置条件不满足时立即停止；达到成本上限立即停止。
- **本地提交要求**：本地提交，提交信息注明依据的核对结论和授权来源。
- **不得推送远程仓库**。

---

## 任务卡5：扩大回归测试

- **背景**：现有真人/物件回归样本量偏小，需要扩大到至少3名真人和4类不透明物件材质。
- **目标**：按《V0.2-回归测试矩阵与授权记录模板.md》采集已授权素材，执行重复生成测试，填写矩阵。
- **前置条件**：至少3名真人测试对象的授权状态已确认为"已授权"；至少4类不透明物件素材已采集。
- **允许修改的文件**：`V0.2-回归测试矩阵与授权记录模板.md`（填写结果）。
- **禁止修改的文件**：`src/`目录下所有代码文件（本任务卡是测试执行，不涉及功能改动）。
- **是否允许生成图片**：是，按矩阵设计执行AI国风重绘生成。
- **输入**：已授权真人素材、常见物件素材。
- **输出**：填写完毕的回归测试矩阵；对应的生成记录（遵循现有`writeGenerationRecord`机制）。
- **验收标准**：
  - [ ] 至少3名真人对象、4类物件材质各完成矩阵设计的重复次数
  - [ ] 每条记录都有对应的`records/`目录下生成记录文件
  - [ ] 授权与合规记录栏全部填写，公开仓库排除检查已确认
- **成本上限**：需在执行前单独设定，按《V0.2-成本与运行环境记录模板.md》记录（可直接在回归测试矩阵"调用次数、耗时与成本记录表"一节中复用该模板结构），本任务卡不预设默认值。
- **停止条件**：达到预设成本上限立即停止；若某素材授权状态无法确认为"已授权"，跳过该素材，不得使用未授权素材。
- **本地提交要求**：本地提交，仅提交矩阵文档和不含真人素材路径的生成记录（真人原始照片文件本身不提交）。
- **不得推送远程仓库**。

---

## 任务卡6：V0.2 最终验收

- **背景**：任务卡1-5完成后，需要形成V0.2整体验收结论。
- **目标**：参照Day6/V0.1验收报告的证据标准，撰写`V0.2-整体验收结论.md`。
- **前置条件**：任务卡1-5均已完成并有明确结论（含"不批准/不纳入/保留实验性支持"这类明确结论，不能是空白或"进行中"）。
- **允许修改的文件**：基于《V0.2-整体验收结论模板.md》产出`V0.2-整体验收结论.md`；`国风手账素材工具_V0.2开发任务书.md`（更新各项最终状态）。
- **禁止修改的文件**：无特殊限制，但任何代码改动需有对应任务卡（1-5）的验收结果支撑，不得在验收阶段顺带新增未经过测试的功能改动。
- **是否允许生成图片**：否（验收阶段应基于已有测试结果撰写结论，不产生新数据）。
- **输入**：任务卡1-5的全部产出物。
- **输出**：`V0.2-整体验收结论.md`。
- **验收标准**：
  - [ ] 每一项V0.2范围条款都有明确的通过/部分通过/不通过/不纳入判断，不得笼统写"基本完成"
  - [ ] 所有量化结论标注数据来源（对应哪张任务卡的哪份记录）
  - [ ] 已知风险和遗留问题单独列出，不掩盖
- **成本上限**：0次生成调用。
- **停止条件**：完成验收报告即停止。
- **本地提交要求**：本地提交，提交信息注明"V0.2整体验收结论初稿，待审核"。
- **不得推送远程仓库**。
