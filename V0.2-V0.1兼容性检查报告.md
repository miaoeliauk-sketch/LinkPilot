# V0.2 阶段：V0.1兼容性检查报告

检查日期：2026-08-04
检查方式：从远程分支 `claude/guofeng-notebook-setup-sg55p4` 最新提交 `6d13103`（与《V0.2-接口兼容性审查报告.md》审查时同一提交，本次确认远程无新提交）拉出一份**只读的 git worktree**（`git worktree add --detach`），在其中执行 `pnpm install` / `pnpm test` / `pnpm typecheck`，检查完毕后已用 `git worktree remove` 清理，**未修改任何 `src/` 文件，未修改正式提示词，未在本仓库主工作区留下改动**。

---

## 一、自动化检查结果（真实执行，非推测）

| 检查项 | 结果 | 详情 |
|---|---|---|
| `pnpm install` | ✅ 成功 | 62个包，含sharp 0.35.3、@types/node 26.1.2、tsx 4.23.1、typescript 7.0.2、vitest 4.1.10，用时约4秒 |
| `pnpm test` | ✅ 全部通过 | 8个测试文件，23个测试用例，全部通过，用时约5.9秒 |
| `pnpm typecheck` | ✅ 通过 | `tsc -p tsconfig.json --noEmit`，退出码0，无报错 |

---

## 二、逐项人工检查结果

| 检查项 | 结果 | 依据 |
|---|---|---|
| `GenerationPlan.prompt` 是否保持字符串 | ✅ 是 | 实际读取 `src/intensity.ts`：`export interface GenerationPlan { ...; prompt: string; ... }`，与《V0.2-接口兼容性审查报告.md》记录一致，未变化 |
| RGBA PNG 输出约定 | ✅ 未变化 | 同一提交，`src/composite.ts` 逻辑与此前审查时一致（详见接口兼容性审查报告第三节） |
| 元数据字段 | ✅ 未变化 | `src/metadata.ts` 的 `GenerationRecord` 类型与此前审查时一致（详见接口兼容性审查报告第二节） |
| 程序化装饰流程 | ✅ 未变化 | `src/composite.ts` 的 `composeDecorations` 逻辑未变 |
| 错误处理 | ✅ 未变化 | CLI层usage字符串抛错+`runCliIfMain`统一捕获的风格未变 |
| 现有记录写入 | ✅ 未变化 | `writeGenerationRecord` 机制未变 |
| V0.1 回归用例是否仍被保留 | ✅ 是 | `pnpm test` 显示8个测试文件（`background-cli.test.ts`、`background.test.ts`、`cli.test.ts`、`composite.test.ts`、`defaults.test.ts`、`intensity-cli.test.ts`、`intensity.test.ts`、`record-cli.test.ts`）全部存在且通过，与接口兼容性审查报告第七节列出的文件清单一致 |

---

## 三、发现的问题

### 问题1：`.gitignore` 当前不包含任何真人素材相关的排除规则

**现状**（实际读取远程仓库根目录 `.gitignore`）：

```
node_modules/
dist/
coverage/
tmp/
*.log
```

只覆盖构建产物和日志，**没有任何针对本地测试素材（真人照片、蒙版、抠图结果等）的排除规则**。

**风险**：不是"已经发生的泄露"（本次检查确认远程仓库当前没有任何真人素材文件被跟踪），而是**缺少结构性防护**——如果后续任务卡3/5执行时素材存放位置不慎落在Git跟踪范围内，且执行者提交前只凭肉眼`git status`检查（人工检查可能遗漏，尤其文件数量多时），没有`.gitignore`兜底，存在真人素材被意外提交的可能性。

**修改建议**（不擅自改动`src/`或`.gitignore`本身，建议交由下一步任务卡执行时一并添加）：

```
# V0.2 本地测试素材（真人照片、蒙版、抠图结果），不进入公开仓库
/local-assets/
/local-assets/**
*.local.png
```

具体目录结构建议见《V0.2-素材蒙版与授权检查规范.md》第九节，命名和路径需要保持一致，避免`.gitignore`规则和实际使用的目录名对不上导致规则形同虚设。

### 问题2：未发现其他兼容性问题

除上述`.gitignore`缺口外，本轮检查（含实际运行测试和typecheck）未发现V0.1现有接口、测试、类型约定被破坏或存在其他风险——这是符合预期的结果，因为V0.2目前尚未对`src/`做任何实际代码改动（任务卡1-3的产出都是文档和独立于`src/`的`scripts/matting-evaluation/`骨架），本次检查确认的是"改动前的基线状态健康"，不是"V0.2改动没有破坏什么"。

---

## 四、结论

V0.1 现有代码在当前检查时点（提交`6d13103`）**测试全部通过、类型检查通过、接口约定未发生变化**。发现的唯一问题是`.gitignore`缺少真人素材排除规则，属于流程性风险而非已发生的问题，建议在真正开始采集真人测试素材（任务卡3发丝/细叶素材、任务卡5真人回归素材）之前补上这条规则，作为任务卡3/5执行前的前置检查项之一。
