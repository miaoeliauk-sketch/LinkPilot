# V0.2 成本与运行环境记录模板

> 目的：任务书"实验成本、调用次数与运行环境记录要求"一节要求所有消耗Codex/gpt-image-2调用的任务都要记录成本和环境信息。此前这个要求只以文字形式写在任务书和回归矩阵里，没有独立可复用的表格。本文件提供统一表格，供任务卡2（如涉及新生成，默认不涉及）、任务卡3（智能抠图，如需生成对照样本）、任务卡4（结构化提示词接入验证）、任务卡5（扩大回归测试）在实际执行时各自复制填写一份，不共用同一份表也不共享预算池——每个任务卡的预算必须独立申请、独立记录，不得互相借用或默认沿用。

---

## 使用方式

1. 每个涉及实际生成调用的任务卡执行前，复制以下表格结构，在对应的产出文档中新增一节（例如任务卡5的回归测试矩阵、任务卡3的测试设计表），不要求单独建文件。
2. 预算上限必须在执行前由用户明确给出具体数字，不得引用其他任务卡或其他阶段（例如首轮16次对照实验）的剩余额度。
3. 执行过程中实时记录，不得事后补填估算数字。

---

## 预算与调用记录表

| 字段 | 说明 | 填写要求 |
|---|---|---|
| 所属任务卡 | 待填 | 必填，注明是任务卡几 |
| 预算上限（次） | 待填 | 必填，执行前由用户明确给出，本模板不预设默认值 |
| 实际使用次数 | 待填 | 实时记录，不得事后估算 |
| 成功次数 | 待填 | — |
| 失败次数 | 待填 | 需拆分失败原因，见下表 |
| 剩余预算（次） | 待填 | 预算上限-实际使用次数 |
| 是否达到预算上限 | 待填 | 达到即停止，不得继续调用 |

---

## 失败原因分类记录表

| 序号 | 时间戳 | 失败原因分类（网络错误/模型输出不合规/其他） | 具体描述 | 是否计入预算（默认是） |
|---|---|---|---|---|
| 待填 | 待填 | 待填 | 待填 | 待填 |

---

## 运行环境记录表

| 字段 | 填写要求 |
|---|---|
| Codex CLI 版本 | 必填 |
| 模型版本（如gpt-image-2） | 必填 |
| 执行设备 | 必填，需注明是本地机器（不得在远程会话中执行实际生成） |
| 执行日期/时间范围 | 必填 |
| 单次调用平均耗时 | 待实测 |
| 累计总耗时 | 待实测 |

---

## 停止条件确认（执行前勾选，作为自我检查）

- [ ] 预算上限已由用户明确给出具体数字（不是"沿用之前的"）
- [ ] 达到预算上限会立即停止，不自行扩大
- [ ] 失败调用默认计入预算，除非任务卡另有明确说明
- [ ] 记录会实时写入，不是事后凭印象回填
