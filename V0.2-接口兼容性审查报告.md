# V0.2 接口兼容性审查报告

审查日期：2026-08-04
审查范围：远程分支 `claude/guofeng-notebook-setup-sg55p4` 最新提交 `6d13103`（V0.1归档版本）的实际源码
审查方式：只读代码审查（`src/types.ts`、`src/metadata.ts`、`src/composite.ts`、`src/background.ts`、`src/intensity.ts`、`src/defaults.ts`、`src/cli.ts`、`src/background-cli.ts`、`src/record-cli.ts`、`src/cli-utils.ts`、`README.md`、`package.json`），未修改任何代码，未运行测试。

结论定位：本报告审查的是 **V0.1 现有代码的接口现状**，目的是给出 V0.2 开发必须遵守的约束清单，供每张 Codex 任务卡引用。因为 V0.2 尚未开始改代码，本报告不是"发现V0.2破坏了什么"，而是"记录V0.2不能破坏什么"。

---

## 一、输入字段

| 接口 | 必填参数 | 可选参数 | 备注 |
|---|---|---|---|
| `pnpm compose`（`src/cli.ts`） | `--input`、`--output`、`--source-type <real\|ai>` | `--manifest`、`--records-dir` | manifest JSON 结构：`{decorations: [{id, widthRatio, opacity, marginRatio, preferredAnchors?, path 或 asset 二选一}]}` |
| `pnpm background`（`src/background-cli.ts`） | `--input`、`--output`、`--preset <none\|grid\|xuan-paper>`、`--source-type` | `--records-dir` | `--input`/`--output`路径不能相同，否则报错 |
| `pnpm intensity`（`src/intensity-cli.ts`，未直接读取，行为以README为准） | `--level <light\|standard\|rich\|all>`、`--source`、`--subject`、`--mapping`、`--output` | — | 只生成JSON计划，不调用AI，不消耗生成额度 |
| `pnpm record`（`src/record-cli.ts`） | `--plan`、`--run-id`、`--output`、`--source-type` | `--model`（默认`gpt-image-2`）、`--records-dir` | 会校验`plan`中存在对应`runId`，并校验`sourceImage`和`output`两张图片均可读 |

**V0.2约束**：任何新增CLI参数应遵循同样的"必填校验缺失即抛出usage字符串错误"约定（见第四节），不要静默使用默认值掩盖参数缺失。

---

## 二、输出字段 / 元数据结构

`src/metadata.ts` 中 `GenerationRecord` 类型：

```ts
interface GenerationRecord {
  input_image: string;
  source_type: "实拍" | "AI测试素材";
  params: Record<string, unknown>;   // 自由字段，可扩展
  output_image: string;
  timestamp: string;                 // ISO字符串
  human_review: "通过" | "不通过" | "待定";
}
```

- 记录文件路径规则：`${输出图所在目录}/records/${输出图文件名（不含扩展名）}-${时间戳（冒号替换为短横线）}-${uuid前8位}.json`，每次运行生成独立文件，**不会覆盖历史记录**。
- `params`是`Record<string, unknown>`，**已经可以自由扩展**，例如浓郁档测量结果（见《V0.2-浓郁档测量规范.md》第十节建议）可以直接作为新字段塞进`params`，不需要改动`GenerationRecord`类型定义本身。

**V0.2约束**：
1. 新增功能（智能抠图、结构化提示词、白斑测量）如果要写入生成记录，优先复用`params`自由字段，不要新增顶层字段，除非有充分理由说明为什么不能放进`params`。
2. `human_review`枚举值固定为三个中文字符串，V0.2不应该引入第四种状态而不做全局评估（例如是否要给智能抠图结果单独增加"待复核"这类新状态，需要专门讨论，不能顺手加）。

---

## 三、RGBA PNG 约定

来自`src/composite.ts`的实际实现，这是当前最核心、最容易被无意打破的一组约定：

1. 输入图片会被`sharp(...).ensureAlpha()`强制转换为4通道，若转换后通道数不是4会直接抛错。
2. 装饰只画在`alpha === 0`（完全透明）的像素上；只要目标像素`alpha > 0`（哪怕只有1），装饰绘制会**跳过该像素**，代码注释明确写"Only paint transparent space. Existing subject pixels remain byte-for-byte unchanged."——这是一个被测试覆盖的硬约束（README列出的测试覆盖包含"主体像素不变"）。
3. 如果输入图完全没有透明像素（`transparentPixels === 0`），直接抛错"输入图片没有透明区域，无法放置装饰"，不会静默跳过。
4. 装饰摆放使用四角碰撞检测（`bottom-right/top-right/top-left/bottom-left`，可通过`preferredAnchors`调整优先级），选择零碰撞的位置；如果四个角都放不下，该装饰会被跳过并记录原因（`skipped`数组），**不会强行裁切图案**。

**V0.2约束（重点）**：
- 智能抠图原型（第2项）如果产出"部分透明"的输出（例如发丝末梢做渐变透明），需要注意：当前装饰合成逻辑对"部分透明但alpha>0"的像素当作"已占用"处理，不会在这类像素上叠加装饰。这对发丝这种边缘本身就该有渐变透明的场景是合理的，但**这一行为此前从未在真实的渐变透明输入上测试过**（V0.1所有素材抠图后基本是非0即255的硬边缘），是否会产生视觉上不理想的结果（例如装饰紧贴在发丝渐变区边缘）需要在原型测试阶段专门验证，不能假设"和V0.1一样能用"。
- 若智能抠图原型最终转正，必须保证输出仍是4通道RGBA，且`ensureAlpha`处理后通道数为4，否则会触发`composeDecorations`的硬错误。

---

## 四、错误处理约定

- CLI层：参数缺失或不合法 → 直接`throw new Error(usageMessage)`，由`runCliIfMain`统一捕获，写入`stderr`并设置`process.exitCode = 1`，**不会让进程静默退出或产生误导性的成功输出**。
- 领域函数层：使用描述性中文错误信息（例如`"${decoration.id}: widthRatio 必须大于0且不超过1"`、`"输出路径不能覆盖原始透明图"`、`"计划中找不到运行编号：${runId}"`），不是抽象的错误码。
- `record-cli.ts`对输入输出图片做`validateImage`校验（能否`stat`为文件、能否被`sharp`读取到`format/width/height`），校验失败会包一层更明确的错误信息（"${label}不可用：${path}"）再抛出。

**V0.2约束**：新增的智能抠图、白斑测量、结构化提示词相关CLI/脚本应延续同样的风格——参数缺失/校验失败直接抛出描述性中文错误并让顶层捕获退出，不要引入新的静默失败或吞异常的模式。

---

## 五、程序化装饰流程

- 完全本地、不依赖AI，逻辑集中在`src/composite.ts`的`composeDecorations`。
- 默认三种装饰（印章/云纹/胶带）定义在`src/defaults.ts`，通过`DecorationAssetId`（`"seal"|"cloud"|"tape"`）与固定PNG素材路径绑定。
- 装饰配置可以通过`--manifest`传入自定义JSON覆盖默认装饰，manifest中每项装饰用`asset`（内置素材ID）或`path`（自定义路径）二选一指定来源。

**V0.2约束**：V0.2不涉及装饰流程本身的功能变更（不在5项范围内），审查未发现风险，仅需确认后续任何pipeline改动（例如插入智能抠图步骤）不改变"装饰在主体透明PNG之上叠加、只画进透明区域"这一既有顺序和约束。

---

## 六、现有风格档位（三档强度）

`src/intensity.ts`关键事实：

- `INTENSITIES = ["light", "standard", "rich"]`，`CHROMA_KEYS = ["#00FF00", "#FF00FF"]`（固定二值联合类型）。
- **五段式提示词模板已经直接硬编码在`buildGenerationPlan`函数内**（模板字面量拼接"场景/背景、主体、细节、用途、限制"五段），不存在独立的"提示词策略"抽象层或可插拔接口。
- `GenerationPlan.prompt`是**字符串类型**，`record-cli.ts`将其原样写入`GenerationRecord.params.prompt`（同样是字符串位置）。
- 三档强度除了提示词措辞不同，还联动控制装饰数量（`decorationCount`）、装饰缩放（`decorationScale`）、装饰不透明度（`decorationOpacity`），这三者是通过`defaultDecorations().slice(0, profile.decorationCount)`加乘法系数实现的，不是独立配置。

**V0.2约束（重点，直接关系第1项范围条款）**：
1. 接入结构化全字段提示词，技术上意味着修改`buildGenerationPlan`内部拼接逻辑，或者新增一个并行函数/参数（例如`promptFormat: "five-segment" | "structured"`）。**这属于正式提示词策略变更，按项目边界第4条，必须等实验记录核对通过并获得明确授权后才能改动这个函数**，本报告只指出改动点在哪里，不建议、不预判应该怎么改。
2. 无论最终采用哪种实现方式，`GenerationPlan.prompt`必须保持字符串类型，否则会破坏`record-cli.ts`里`params.prompt`的写入约定（该字段类型是`Record<string, unknown>`，技术上能塞对象，但会破坏现有"prompt是完整文本"的语义，下游任何读取`params.prompt`当字符串处理的代码都会出问题）。
3. `CHROMA_KEYS`目前是固定的双值联合类型，若智能抠图原型（第2项）需要绕开色度键（例如发丝直接用原图背景做智能抠图，不生成色度键中间图），这是对现有pipeline"必须先生成色度键背景"这一假设的架构级例外，需要单独设计，不能默认套用现有`CHROMA_KEYS`类型和`buildGenerationPlan`的背景描述逻辑。

---

## 七、V0.1 已通过回归用例

依据`README.md`"验证"章节列出的测试覆盖范围（`pnpm test`即vitest套件）：

- 画布尺寸不变
- 透明通道保留
- 主体像素不变
- 装饰不越界
- 透明区域出现装饰
- 默认3种装饰可以共同合成
- 3种背景（无/方格纸/宣纸）自由切换
- 原始透明图不被覆盖
- 三档提示词与装饰参数存在明确差异

**V0.2约束**：以上9项是V0.2开发期间必须保持通过的最低回归基线。任何V0.2任务卡的验收标准都应包含"`pnpm test`全部通过，且以上9项对应用例未被删除或弱化"，具体测试文件为`tests/*.test.ts`（`background-cli.test.ts`、`background.test.ts`、`cli.test.ts`、`composite.test.ts`、`defaults.test.ts`、`intensity-cli.test.ts`、`intensity.test.ts`、`record-cli.test.ts`）。

---

## 八、审查结论汇总

| 审查项 | 结论 |
|---|---|
| 是否发现V0.1现有接口已被破坏 | 否（V0.2尚未开始改代码，本报告审查的是现状） |
| 最大兼容性风险点 | 五段式提示词硬编码在`buildGenerationPlan`内，接入结构化格式必然涉及该函数改动，且受项目边界第4条约束，暂不批准 |
| 次要风险点 | 智能抠图若产出渐变透明边缘，需验证装饰合成逻辑在这种输入上的实际表现（此前未测试过） |
| 次要风险点 | `CHROMA_KEYS`固定双值类型，若智能抠图绕开色度键流程，是架构级例外，需单独设计 |
| 需要延续的既有约定 | CLI参数校验风格、错误信息风格、`GenerationRecord.params`自由扩展字段的使用方式 |
