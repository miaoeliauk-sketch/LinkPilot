# V0.2 智能抠图原型测试设计表（发丝 / 细叶）

> 对应任务书第2项，主状态：`原型验证，待实测`（固定文字，与《国风手账素材工具_V0.2开发任务书.md》保持一致）。
> 目的：为"发丝、细叶智能抠图原型"（V0.2范围第2项）设计一套不依赖新图片即可先行准备的测试方案。当前只有表格结构和方法规则，不含任何实测数据。所有"结果"列在真实测试完成前一律留空或写"待实测"，禁止填入推测值。
> 明确边界：本测试的最终目的是给出"转正式功能 / 保留实验性支持 / 继续排除"三选一的决策依据。**测试表结构本身完成、方法规则本身定稿，都不代表智能抠图已经可用——这只是测试设计工作的完成，不是测试结果，主状态不会因为本文件被修订而改变。**
> 本文件中出现的所有具体数值（alpha阈值、评分门槛、决策门槛等）都是**测试设计阶段的预设值**，用于保证测试可执行、可复现、评分不临场发挥，**不是实测数据，也不是最终定论**——文末专门列出其中仍建议由你在实测前确认或调整的参数。

---

## 一、素材编号栏

| 编号 | 类型 | 素材描述（拍摄/采集后填写） | 来源（实拍/AI测试素材） | 授权状态 | 本地路径（不进公开仓库） |
|---|---|---|---|---|---|
| HAIR-01 | 发丝 | 待填 | 待填 | 待填 | 待填 |
| HAIR-02 | 发丝 | 待填 | 待填 | 待填 | 待填 |
| HAIR-03 | 发丝 | 待填 | 待填 | 待填 | 待填 |
| LEAF-01 | 细叶 | 待填 | 待填 | 待填 | 待填 |
| LEAF-02 | 细叶 | 待填 | 待填 | 待填 | 待填 |
| LEAF-03 | 细叶 | 待填 | 待填 | 待填 | 待填 |

### 授权状态允许值（固定枚举，不得自由填写其他文本）

| 允许值 | 含义 | 是否可进入第三节对照测试 |
|---|---|---|
| `已授权` | 真人素材，已获得本人书面或口头同意（需在"素材描述"栏注明确认方式），或用户自行拍摄的非真人素材 | 可以 |
| `AI测试素材` | 非真人素材，或AI生成的测试素材，不涉及真人授权问题 | 可以 |
| `未授权-不可用` | 授权状态未确认，或已知未获得授权 | **不可以**，该素材编号整行数据保留在本表用于记录曾评估过、但不得填入第三节对照测试表，也不得用于计算任何统计结果 |

> 授权状态为空、写"待定"、"可能可以"等模糊表述，一律按`未授权-不可用`处理，不得放行。这一栏必须先于本表其他所有栏目确认完毕。

---

## 二、样本覆盖最低要求（硬性要求，非建议）

| 类别 | 最低覆盖要求 | 覆盖检查（实测前逐项确认） |
|---|---|---|
| 发丝 | 至少1个深色发丝样本 + 至少1个浅色/挑染发丝样本，合计≥3组 | ☐ 深色发丝已覆盖　☐ 浅色/挑染发丝已覆盖 |
| 细叶 | 至少1个细密复叶样本（如Day3 #9同类绿植）+ 至少1个较稀疏叶片样本，合计≥3组 | ☐ 细密复叶已覆盖　☐ 稀疏叶片已覆盖 |

> 原因：Day3结论确认暗色发丝会加重排线/抠图问题、叶片密度影响误删比例，若不强制覆盖两种子类型，测试结果可能只反映"容易的那一种"，不能代表整体结论。**覆盖检查表未全部勾选前，本轮测试不得视为完整，不得据此填写第八节最终决策。**

---

## 三、对照测试记录表（普通色度键 vs 智能抠图）

**重复次数为硬性要求**：每个素材编号、每条路线（色度键 / 智能抠图）**必须获得至少3次成功且可评分的运行结果**，不是建议。

**失败运行的统计方式（固定，不得删除或跳过不记）**：

- 运行过程中出现崩溃、超时、或输出不可用（如输出文件损坏、无法读取、明显非预期结果）等情况，**一律判定为失败运行，必须在表中占一行完整记录**：运行序号照常编号（不因为失败就跳过该序号或事后删除），失败原因按《V0.2-错误分类与错误映射.md》的8类固定分类（素材问题/工具崩溃/超时/输出损坏/输出不可用/依赖缺失/内存不足/其他）填写，其余量化指标列填"失败-不适用"，不得留空、不得删除该行。
- **允许补跑**：若某次运行失败，可以额外补跑以凑够3次成功结果，但补跑的次数本身也要完整记录（例如运行序号可以到#4、#5），不能只保留最终凑够的3次成功记录而把失败的#1/#2抹掉。
- **失败次数不得从任何统计分母中被悄悄剔除**——失败次数如何计入统计，见第八节"0分结果占比和失败率的分母"定义。
- **停止条件（双重，任一先满足即停止，消除歧义）**：同一素材、同一路线**最多允许5次失败尝试**（按失败尝试总次数计算，不是"补跑次数"——即从第一次运行开始，凡是判定为失败的尝试都计入这5次上限，不因为它是"第一次运行"还是"第N次补跑"而区别对待，避免用"补跑次数"这个口径反复解释、变相突破上限）；达到5次失败，**或**达到本任务卡的预算上限（见任务卡3成本上限、《V0.2-成本与运行环境记录模板.md》），**任一条件先满足即停止**，不得等两个条件都满足才停。若停止时仍未获得3次成功且可评分结果，如实记录"该素材该路线未能获得3次有效结果"，该素材**不纳入第八节最终决策的统计依据**，只能作为探索性观察单独注明，不得强行放宽"成功"的判定标准来凑数。

| 素材编号 | 路线 | 运行序号 | 核心保留率 | 细节完全不透明保留率 | 细节半透明占比 | 细节完全透明丢失率 | 明显误抠面积 | 细节误抠面积 | 边缘白边/污染等级(0-3) | 冷启动耗时 | 热运行耗时 | 峰值内存/显存 | 失败原因（如有，失败行必须保留） |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| HAIR-01 | 色度键（基准） | #1/#2/#3(+补跑) | 待实测 | 待实测 | 待实测 | 待实测 | 待实测 | 待实测 | 待实测 | 待实测 | 待实测 | 待实测 | 待实测 |
| HAIR-01 | 智能抠图 | #1/#2/#3(+补跑) | 待实测 | 待实测 | 待实测 | 待实测 | 待实测 | 待实测 | 待实测 | 待实测 | 待实测 | 待实测 | 待实测 |
| HAIR-02 | 色度键（基准） | #1/#2/#3(+补跑) | 待实测 | 待实测 | 待实测 | 待实测 | 待实测 | 待实测 | 待实测 | 待实测 | 待实测 | 待实测 | 待实测 |
| HAIR-02 | 智能抠图 | #1/#2/#3(+补跑) | 待实测 | 待实测 | 待实测 | 待实测 | 待实测 | 待实测 | 待实测 | 待实测 | 待实测 | 待实测 | 待实测 |
| HAIR-03 | 色度键（基准） | #1/#2/#3(+补跑) | 待实测 | 待实测 | 待实测 | 待实测 | 待实测 | 待实测 | 待实测 | 待实测 | 待实测 | 待实测 | 待实测 |
| HAIR-03 | 智能抠图 | #1/#2/#3(+补跑) | 待实测 | 待实测 | 待实测 | 待实测 | 待实测 | 待实测 | 待实测 | 待实测 | 待实测 | 待实测 | 待实测 |
| LEAF-01 | 色度键（基准） | #1/#2/#3(+补跑) | 待实测 | 待实测 | 待实测 | 待实测 | 待实测 | 待实测 | 待实测 | 待实测 | 待实测 | 待实测 | 待实测 |
| LEAF-01 | 智能抠图 | #1/#2/#3(+补跑) | 待实测 | 待实测 | 待实测 | 待实测 | 待实测 | 待实测 | 待实测 | 待实测 | 待实测 | 待实测 | 待实测 |
| LEAF-02 | 色度键（基准） | #1/#2/#3(+补跑) | 待实测 | 待实测 | 待实测 | 待实测 | 待实测 | 待实测 | 待实测 | 待实测 | 待实测 | 待实测 | 待实测 |
| LEAF-02 | 智能抠图 | #1/#2/#3(+补跑) | 待实测 | 待实测 | 待实测 | 待实测 | 待实测 | 待实测 | 待实测 | 待实测 | 待实测 | 待实测 | 待实测 |
| LEAF-03 | 色度键（基准） | #1/#2/#3(+补跑) | 待实测 | 待实测 | 待实测 | 待实测 | 待实测 | 待实测 | 待实测 | 待实测 | 待实测 | 待实测 | 待实测 |
| LEAF-03 | 智能抠图 | #1/#2/#3(+补跑) | 待实测 | 待实测 | 待实测 | 待实测 | 待实测 | 待实测 | 待实测 | 待实测 | 待实测 | 待实测 | 待实测 |

> 每个"素材编号×路线"实际需要的行数不固定为2行——若出现失败需要补跑，按第三节上方"失败运行的统计方式"规则增加行，不得覆盖或删除失败的行。

### 汇总统计栏（每个"素材编号×路线"3次重复完成后填写，仍是待实测）

| 素材编号 | 路线 | 平均耗时（热运行） | 最大耗时（热运行） | 三次核心/细节完全不透明保留率标准差（稳定性） |
|---|---|---|---|---|
| … | … | 待实测 | 待实测 | 待实测 |

### 耗时与资源记录方式（含记录条件）

- **冷启动耗时**：该路线在当前进程/会话中**第一次**运行的总耗时，包含模型加载、依赖初始化等一次性开销，单独记录，不计入"平均耗时"。
  - **记录条件**：必须是全新启动的进程/会话，运行前不得有该路线的任何预热（不能先跑一次"暖机"再假装这是冷启动）；若测试环境本身有常驻缓存（如模型权重缓存在磁盘或内存中），需在环境记录表中注明缓存是否清空，未清空的冷启动耗时需要标注"含缓存"，不能与真正首次运行的数字混在一起比较。
- **热运行耗时**：同一进程/会话内，冷启动之后的重复运行耗时（即3次重复中的第2、3次，及之后的额外重复），只用热运行数据计算平均耗时和最大耗时，避免冷启动开销拉高平均值、误导对运行成本的判断。
  - **记录条件**：必须在同一进程/会话内连续运行，中途不重启进程、不清缓存；若中途重启了进程，该次运行应重新计为"冷启动"，不得算作热运行。
- **平均耗时**：该素材该路线全部热运行耗时的算术平均。
- **最大耗时**：该素材该路线全部热运行耗时中的最大值（用于评估最坏情况，例如是否会导致用户等待过久）。
- **峰值内存/显存**：运行期间监控到的最高占用值。
  - **记录条件**：①需注明测量工具（如`/usr/bin/time -v`、`nvidia-smi`、或其他系统监控工具），工具不同则数字之间不可直接比较，需在记录中注明所用工具；②监控必须覆盖从进程启动到进程结束的**全程**，取全程最大值，不得只在若干采样点估算峰值；③记录期间设备上不应同时运行其他高负载任务（如另一个生成任务、大型编译等），否则峰值读数会被干扰，若无法避免同时运行其他任务，需在记录中注明，该次峰值数据标记为"可能受干扰，仅供参考"。

### 运行环境记录表（每轮测试固定记录一次；同一轮测试内所有素材共用同一份环境记录，除非中途更换设备或升级依赖版本——一旦更换需要重新记录一份，并在对照测试记录表中注明从哪个运行序号开始使用新环境，避免不同环境的数字被当作同一批比较）

| 字段 | 填写要求 |
|---|---|
| 测试设备型号 | 必填（如具体品牌型号，或"MacBook Pro 14-inch 2023"这类可辨识描述） |
| 操作系统及版本 | 必填（如 macOS 14.5 / Ubuntu 22.04） |
| Node.js 版本 | 必填（与`package.json`引擎要求对照，`node -v`输出） |
| Python 版本（如智能抠图工具依赖Python） | 必填，若不涉及Python则填"不适用" |
| 智能抠图所用模型/库名称及版本号 | 必填，需精确到具体版本号（如某matting库的release tag或commit hash），不能只写"最新版" |
| CPU型号 | 必填 |
| 内存容量 | 必填 |
| GPU型号（如有） | 必填，若无GPU则填"无，纯CPU运行" |
| 显存容量（如有） | 必填，若无GPU则填"不适用" |
| 记录时间 | 必填（该份环境记录对应的测试起止日期） |

> 以上字段是"每轮测试要填的表头"，当前均未填写、不含任何数据；实测执行时必须先填完这张表，再开始跑第三节的对照测试记录表。

---

## 四、目标区域参考蒙版规范

### 制作方式（固定，非"待定"，两种路径均可）

- **路径A：人工标注**。使用人工标注工具在原图上圈出主体轮廓，导出为与原图同尺寸的单通道蒙版：255=主体，0=背景。工具不限定具体软件（GIMP/Photoshop/其他画图工具的选区+导出功能均可）。
- **路径B：算法辅助标注**（新增，见`scripts/matting-evaluation/generate-reference-mask.ts`）。适用于源图背景是纯色度键色的情况：程序按色度距离把像素分三档（高置信背景/高置信前景/模糊待复核），模糊区域（集中在发丝末梢、叶尖）不自动下判断，只在复核叠加图里标红，**必须人工逐一核对模糊区域的默认归类后才能冻结**，不能把程序输出的草稿直接当正式蒙版。选这条路径的动机：比纯人工描边更准确、更容易复现，且能正确处理主体轮廓内部的镂空/孔洞（如龟背竹类叶片的自然裂口），人工描边容易漏描或多描这类内部孔洞。
- 无论哪种路径，导出格式和数值约定固定为二值：255=主体，0=背景。每个素材编号需要分别标注两份蒙版：**核心区域蒙版**（头部/躯干主干等大块结构）+ **细节区域蒙版**（发丝末梢/叶片边缘等细碎结构），两者可以有重叠但不要求互斥，分别用于计算核心保留率和细节完全不透明保留率。
- **两条路径不是互相竞争的备选项，选哪条取决于素材类型**：源图背景干净、色度键均匀的，优先用路径B（更快更准）；背景复杂、非纯色度键、或路径B跑出来模糊区域占比过高（超过某个比例，暂无固定数值，需人工判断是否"太模糊以至于程序初判没有参考价值"）的素材，改用路径A。

**关于路径B自动分档阈值的说明（测试设计阶段预设值，未在真实素材上校准，待你用真实图核对）**：色度距离背景上限40、前景下限140（0-441.7欧氏距离量级）、核心腐蚀半径6像素，是`generate-reference-mask.ts`当前的默认参数，已用合成测试图（含镂空孔洞场景）验证机制本身没有bug（16个测试用例通过），但没有用真实的HAIR/LEAF生成图验证这三个数字是否合适——如果模糊区域占比明显偏高或偏低，需要调整这三个参数重新生成草稿，不能直接采信默认值算出的结果。

### 复核方式（固定，区分两种路径，优先级不同）

> 注意与上一节"制作方式"的路径A/B区分：本节的"双人独立标注/单人间隔复标"是**制作方式路径A（人工标注）**专用的复核机制（比较两次独立标注的IoU）。若使用**制作方式路径B（算法辅助标注）**，复核方式改为"人工核对review-overlay标红的模糊区域"，不走IoU比较流程——因为路径B的高置信背景/前景部分是确定性算法输出，两次运行结果完全一致，比较IoU没有意义，需要复核的只有模糊区域这一小部分。路径B的复核同样建议双人独立核对模糊区域（优先）或单人间隔复核（降级方案，需注明限制），核对不一致时以更保守的判定（判为需要进一步人工描边确认，而不是随意二选一）为准。

**双人独立标注为优先方案；若项目资源不具备双人条件，可使用单人间隔复标，但最终报告必须明确该限制，且不能把单人复标结果表述为双人一致性验证。没有第二名标注者不构成测试无法开始的硬阻塞。**

复核用来发现两类不同的误差，不能互相替代，需要在记录中明确写出实际使用的是哪一种：

| 路径 | 做法 | 能发现的问题 | 优先级 |
|---|---|---|---|
| **双人独立标注**（优先） | 两名不同标注者，互不参考对方结果，各自独立标注一次 | 能发现"标注者个人偏见/理解差异"（例如对"发丝末梢算不算主体"的判断标准不同） | 优先使用，条件允许时必须用这种 |
| **单人间隔复标**（备选） | 只有一名标注者时，同一人相隔一段时间（建议≥1天，避免记忆残留导致复标时"照抄"第一次）标注两次 | 只能发现"同一人的随机操作误差"，**不能**发现标注标准本身的系统性偏差 | 仅在没有第二名标注者时使用，且必须在记录中注明"本次蒙版使用单人间隔复标，未经双人独立验证"，作为该蒙版可信度的已知限制 |

- 无论哪种路径，都比较两次标注的重叠率（IoU，交并比）。
- 若重叠率≥98%，取其中一次（或两次的并集/交集，标注时统一选定一种合并方式并在记录中注明）作为最终蒙版。
- 若重叠率<98%，判定为标注分歧过大，需要第三次标注仲裁，或人工复核后重新标注，不得直接采用重叠率较低的两次标注中的任意一次。
- 每份最终蒙版需要在记录中注明实际使用的是"双人独立标注"还是"单人间隔复标"，两者不得混用于同一份蒙版的两次标注（即不能第一次找人标、第二次自己标，这样重叠率比较的意义会不清晰）。

### 冻结规则（固定）

- 每份最终蒙版标记版本号（从v1开始）和生成时间戳，标注完成并通过复核后即锁定，测试期间**不得为了让某条路线的结果更好看而重新调整蒙版**。
- 如确实发现蒙版标注错误（如描边有明显失误），必须：①重新走上述复核流程；②版本号升级（v2、v3……）；③在测试记录中注明修改原因和修改时间；④凡是已经用旧版本蒙版计算过的历史结果，需要用新版本蒙版重新计算，不能新旧版本混用于同一份对照结果中。

---

## 五、测量口径与预处理规则（固定，与《V0.2-浓郁档测量规范.md》保持一致，避免V0.2内部口径不统一）

| 项目 | 固定规则 |
|---|---|
| Alpha阈值 | `alpha ≥ 250` 判定为"完全不透明像素"；`alpha ≤ 5` 判定为"完全透明/背景像素"；`6 ≤ alpha ≤ 249` 判定为"半透明过渡像素"。**三档互斥且穷尽（覆盖0-255全部取值），核心/细节区域的每个像素必属于且只属于其中一档**。半透明像素单独统计占比，既不计入"保留"，也不计入"丢失"，不得把半透明像素简单归并进保留或丢失任何一边——具体如何在核心/细节完全不透明保留率、丢失率公式中使用，见第六节 |
| 输入/输出尺寸 | 统一缩放到pipeline最终输出尺寸 1024×1024，与《V0.2-浓郁档测量规范.md》一致，不使用模型或工具的原始输出尺寸，避免分辨率不同导致占比不可比 |
| 色彩空间 | 统一使用sRGB，与DESIGN.md色板定义及《V0.2-浓郁档测量规范.md》保持一致 |
| 缩放算法 | 缩放到统一尺寸时，需使用与生产pipeline相同的算法（当前为sharp库resize，具体插值方式需在实现测量脚本时注明），不得对色度键结果和智能抠图结果使用不同的缩放算法 |
| 预处理限制 | 测量前不得对抠图结果做额外的降噪、锐化、边缘平滑等后处理，必须使用工具/路线的原始直接输出，否则会掩盖真实的误抠/污染情况 |
| 对齐基准 | 参考蒙版与抠图结果必须在同一分辨率、同一裁切/留白基准下逐像素比对，不允许为了对齐而拉伸或裁切任意一方 |

---

## 六、评价维度与计算公式

> 命名说明：本文件"细节完全不透明保留率"（原称"细节保留率"，已改名以消除歧义）**仅指 alpha≥250 的完全不透明像素占比，不代表所有视觉上仍存在的半透明细节**。智能抠图对发丝末梢产生自然的渐变透明是预期效果，这部分会落入下面单独统计的"细节半透明占比"，不会被"细节完全不透明保留率"计入，也不会被"细节完全透明丢失率"计入——查看结果时三个数字必须一起看，只看"细节完全不透明保留率"一个数字会让智能抠图的自然渐变效果被严格指标低估。

| 维度 | 定义 | 计算公式 |
|---|---|---|
| 核心保留率 | 核心区域蒙版中，被抠图结果判定为"完全不透明像素"（alpha≥250）覆盖的比例。核心区域预期不应有大量半透明，仍用严格口径 | `核心保留率 = 核心区域蒙版 ∩ (alpha≥250)区域 的像素数 / 核心区域蒙版总像素数 × 100%` |
| 细节完全不透明保留率 | 细节区域蒙版中，被抠图结果判定为"完全不透明像素"（alpha≥250）覆盖的比例——即"完整保留"，不含半透明部分 | `细节完全不透明保留率 = 细节区域蒙版 ∩ (alpha≥250)区域 的像素数 / 细节区域蒙版总像素数 × 100%` |
| 细节半透明占比 | 细节区域蒙版中，落在半透明过渡区间（6≤alpha≤249）的比例。**单独统计，不计入细节完全不透明保留率，也不计入细节完全透明丢失率**——半透明可能是发丝末梢等合理的渐变效果，不应被直接当作"坏结果" | `细节半透明占比 = 细节区域蒙版 ∩ (6≤alpha≤249)区域 的像素数 / 细节区域蒙版总像素数 × 100%` |
| 细节完全透明丢失率 | 细节区域蒙版中，被判定为"完全透明/背景像素"（alpha≤5）的比例，即真正意义上"完全没保留"的部分。**不是"100%−细节完全不透明保留率"**（那样会把合理的半透明也算作丢失），而是单独用alpha≤5的像素直接计算 | `细节完全透明丢失率 = 细节区域蒙版 ∩ (alpha≤5)区域 的像素数 / 细节区域蒙版总像素数 × 100%` |
| 明显误抠面积 | 抠图结果中被判定为"主体不透明像素"、但不属于核心区域蒙版也不属于细节区域蒙版的像素（整体假阳性） | 绝对像素数 = `抠图结果不透明区域 − (核心区域蒙版 ∪ 细节区域蒙版)` 的像素数；占比 = 该像素数 / 图像总像素数 × 100% |
| 细节误抠面积 | 明显误抠面积中，落在"细节区域蒙版外扩缓冲带"（细节区域蒙版向外膨胀5像素形成的邻域）内的部分，专门衡量发丝末梢/叶片边缘周围的噪点式误抠，与"明显误抠面积"（可能来自图像其他位置）区分开 | 绝对像素数 = `明显误抠面积像素 ∩ 细节区域蒙版外扩5像素缓冲带`；占比 = 该像素数 / 细节区域蒙版外扩缓冲带总像素数 × 100% |
| 边缘白边/污染等级 | 0=无可见污染，1=轻微色边，2=明显色边但不影响可用性，3=严重污染影响可用性 | 人工目测评级，需两人独立评级取一致或记录分歧 |
| 重复运行稳定性 | 同一素材同一路线3次重复运行，核心/细节完全不透明保留率的波动程度 | 三次核心保留率的标准差、三次细节完全不透明保留率的标准差，分别记录 |
| 失败原因 | 若某次运行失败，分类记录原因 | 分类：见《V0.2-错误分类与错误映射.md》8类（素材问题/工具崩溃/超时/输出损坏/输出不可用/依赖缺失/内存不足/其他） |

> 三者关系：`细节完全不透明保留率 + 细节半透明占比 + 细节完全透明丢失率 = 100%`（对同一份细节区域蒙版而言，三档alpha区间互斥且穷尽）。报告结果时三个数字必须同时给出，不能只报"细节完全不透明保留率"一个数字就下结论——例如"细节完全不透明保留率60%、细节半透明占比35%、细节完全透明丢失率5%"和"细节完全不透明保留率60%、细节半透明占比5%、细节完全透明丢失率35%"这两种情况，保留率数字相同，但实际效果（前者是渐变自然，后者是大量真丢失）完全不同。
>
> "外扩5像素"这个缓冲带宽度是测试设计阶段的预设值，不是标准定义，若实测后发现5像素明显偏大或偏小（例如把过多正常细节误判成"细节误抠"），可以调整，但调整后需要在报告中注明并对已有数据重新计算，不能新旧口径混用。

---

## 七、0-3级评分标准与量化指标对应关系（预设阈值，待实测前确认）

**区间改为不重叠、无空档（左闭右开，消除边界重叠和未覆盖区间）：**

| 等级 | 含义 | 核心保留率 | 细节完全不透明保留率 | 细节误抠面积占比 | 边缘污染等级 |
|---|---|---|---|---|---|
| 3 | 良好：核心和细节区域均保留良好，边缘干净，无需或极少人工修补 | ≥99.5% | ≥95% | <1% | =0 |
| 2 | 基本可用：核心区域完整，细节区域部分保留，少量人工修补即可用 | ≥99.5% | [80%, 95%) | 无硬性上限，但需人工目测确认无明显误抠 | ≤1 |
| 1 | 勉强可用：核心区域完整，但细节区域大量丢失或污染，需要大量人工修补 | ≥95% | [40%, 80%) | — | ≤2 |
| 0 | 不可用：核心区域即出现明显缺损或严重污染 | <95% | <40% | — | =3 |

> 0分的判定不要求"核心保留率<95%"和"细节完全不透明保留率<40%"同时满足——**任一条件单独成立即为0分**：核心保留率<95%，或细节完全不透明保留率<40%，或边缘污染等级=3，或出现结构性缺损（如整段发丝/整片叶子消失），四者任一触发都直接判0分。

**取最低等级规则（必须遵守）**：单次结果的核心指标、细节指标、误抠面积、污染等级可能分别落在不同等级的门槛区间内（例如核心指标达到3分档，但细节指标只够1分档）。**每次结果的最终等级取所有适用条件中的最低等级；若任一硬性缺陷触发0分，则直接记为0分**，不得用"平均"或"就高不就低"的方式折中。

**细节半透明占比作为决策必看项（不只是附加参考）**：评分时，若细节完全不透明保留率未达到更高档位、但差额主要落在"细节半透明占比"而非"细节完全透明丢失率"，**评分本身仍严格按上表门槛判定，不得因半透明看着自然就跳档**；但在第八节最终决策阶段，半透明占比必须作为必查数据一并报告和讨论（不能只报"细节完全不透明保留率"就下转正/排除结论），具体见第八节决策项表新增行。

> 评分只能在真实测试完成、且按上表门槛对照量化数据判定之后填写，不能凭印象直接打分。区间已改为不重叠且无空档，理论上不应再出现"模糊地带"；若实际测试中仍出现表未覆盖的数值组合，按"取最低等级"规则处理，并在第九节参数清单中反馈，用于后续修订门槛。

---

## 八、最终决策栏（测试完成后填写）

### 判定门槛（预设值，待实测前确认）

**关于"细节完全不透明保留率均值提升"的口径（必须遵守，不得用逐像素合并的方式偷换概念）**：

- **宏平均**：先对每个素材分别计算"智能抠图细节完全不透明保留率 − 色度键细节完全不透明保留率"的提升幅度，再对所有素材的提升幅度取算术平均，得到总体提升幅度。**不得**把所有素材的像素直接合并在一起算一个总体保留率再相减（微平均），因为素材难度不同，微平均会被像素数量多的素材主导，掩盖单个素材的真实表现。
- **必须同时保留逐素材、逐类型（发丝 / 细叶）结果**：总体宏平均只是一个汇总数字，报告和最终决策时必须附上每个素材的提升幅度，以及发丝类、细叶类各自的类内平均提升幅度，不能只给一个总体数字就下结论。

| 决策 | 判定条件（需同时满足） |
|---|---|
| **转正式功能** | ① 第二节样本覆盖检查表已全部勾选完成；② 所有素材×路线均完成≥3次成功且可评分的运行；③ 智能抠图路线全部评分的平均分 ≥ 2.5；④ 智能抠图路线**没有出现0分结果**；⑤ 相对色度键路线，细节完全不透明保留率**宏平均**提升 ≥ 20个百分点；⑥ **发丝类、细叶类分别检查**：任一类型的类内平均提升幅度不得为负——**即使总体宏平均提升达标，只要发丝类或细叶类中任一类型相对色度键路线出现退化（提升幅度为负），也不得判定为转正式功能**，此时应改为"保留实验性支持（仅限某类型）"或单独评估该类型是否要排除；⑦ 重复运行稳定性达标：核心/细节完全不透明保留率的标准差 < 3%；⑧ **失败率必须由用户明确判断为可接受**——本文件暂不预设失败率的具体数值门槛（是否设数值本身也待确认，见第九节），但"转正式功能"不能只满足"最终凑够3次成功"就算数：决策时必须同时列出该路线的实际失败率数字和失败原因分类，由用户看着这些数字明确判断"可接受"，不能因为形式上满足3次成功就默认失败率无所谓；⑨ 运行成本（耗时、资源占用）由用户人工确认为可接受（不预设数值门槛，因为"可接受"取决于实际产品场景，不适合在测试设计阶段替用户决定） |
| **保留实验性支持** | 平均分在 1.5~2.5 之间，或存在0分结果但占比 < 20%（占比定义见下），或总体宏平均有提升但某一类型（发丝/细叶）退化，或相对色度键有提升但稳定性未达标，或失败率人工判定为"有条件可接受"，或运行成本人工判定为"有条件可接受"。若是因为"某一类型退化"进入此档，需在决策依据中注明具体是哪个类型退化，以及退化幅度 |
| **继续排除** | 平均分 < 1.5，或0分结果占比 ≥ 20%，或失败率人工判定为不可接受，或相对色度键路线没有实质提升（宏平均细节完全不透明保留率提升 < 20个百分点），或运行成本人工判定为不可接受 |

**0分结果占比和失败率的分母（必须明确，不得含糊）**：

- **0分结果占比**：分母 = 智能抠图路线**全部成功且可评分**的运行次数（不含失败运行，因为失败运行本身没有评分，无法判定是不是"0分"）；分子 = 其中评分结果为0分的次数。
- **失败率**：分母 = 智能抠图路线**全部尝试次数**（含失败+成功，即第三节表格中该路线实际出现的所有行数，包括补跑的行）；分子 = 其中判定为失败（按《V0.2-错误分类与错误映射.md》8类分类，"依赖缺失"是否计入分子按该文件第三节"视情况"处理）的次数。**失败率和0分结果占比是两个独立指标，分母不同，不能混用**——失败率高说明"经常跑不出结果"，0分占比高说明"跑出来了但质量不可用"，两者反映不同的问题，决策时需要分别看。

**失败率的判定方式（本轮修订新增，之前是缺口）**：本文件**暂不预设失败率的具体数值门槛**（例如"失败率超过X%则不可接受"这种数字目前没有依据凭空定），改为：决策时必须把实际失败率数字、失败原因分类（《V0.2-错误分类与错误映射.md》8类）一并列入决策依据，由用户在看到这些数字后人工判断"可接受/有条件可接受/不可接受"三档，不允许决策依据里只字不提失败率就直接给结论。是否要在后续（例如看到首批预实验结果后）改为固定数值门槛，列入第九节待确认参数。

| 决策项 | 结果 |
|---|---|
| 样本覆盖检查表是否全部完成 | 待实测 |
| 每组素材是否均获得≥3次成功且可评分的运行 | 待实测 |
| 智能抠图路线平均评分 | 待实测 |
| 是否出现0分结果（数量 / 占比，分母=全部成功可评分次数） | 待实测 |
| 失败率（分母=全部尝试次数，含失败+成功）及失败原因分类明细 | 待实测 |
| 失败率人工判定：☐可接受　☐有条件可接受　☐不可接受 | 待实测 |
| 细节半透明占比汇总（逐素材，必查项，不得省略） | 待实测 |
| 相对色度键细节完全不透明保留率提升幅度——**宏平均**（总体） | 待实测 |
| 相对色度键细节完全不透明保留率提升幅度——发丝类（类内平均） | 待实测 |
| 相对色度键细节完全不透明保留率提升幅度——细叶类（类内平均） | 待实测 |
| 是否存在任一类型退化（提升幅度为负） | 待实测 |
| 逐素材提升幅度明细（每个素材编号单独列出，不得只给汇总数字） | 待实测 |
| 重复运行稳定性（标准差）是否达标 | 待实测 |
| 运行成本（耗时/资源）是否可接受（人工判断） | 待实测 |
| **最终决策**：☐ 转正式功能　☐ 保留实验性支持（含"仅限某类型"的情况）　☐ 继续排除 | 待实测 |
| 决策依据摘要 | 待实测 |

> 明确提醒：即使数据满足"转正式功能"门槛，是否真正转正仍需单独评估工程成本（是否需要新依赖、是否影响现有pipeline的RGBA约定，尤其是渐变透明边缘对装饰合成逻辑的影响，参见《V0.2-接口兼容性审查报告.md》第三节），不能仅凭本表的量化门槛自动决定，本表门槛是"允许进入转正讨论"的必要条件，不是充分条件。

---

## 九、参数处理状态清单

以下10项参数，经本轮修订已按你的明确意见逐项处理，状态分三种：**按预设值执行**（先用文件里的数字试跑，正式测试前/看到预实验结果后再确认一次）、**已修正**（本轮已按你的意见改过公式/规则本身）、**待补充**（规则已定但数值本身还空着，明确留到看到预实验结果后再定，不是遗漏）。

| # | 参数 | 状态 | 说明 |
|---|---|---|---|
| 1 | Alpha阈值（250/5，第五节） | 按预设值执行 | 先用于试跑，正式测试前需再次确认是否符合实际抠图工具的输出特征 |
| 2 | 细节区域外扩缓冲带宽度（5像素，第六节） | 按预设值执行 | 必须保持全批次一致，不得中途更换宽度而不重算历史数据 |
| 3 | 0-3级评分门槛（第七节） | **已修正** | 本轮已改为不重叠、无空档的区间（左闭右开），并加了"取最低等级"规则，不再需要"模糊地带按更保守等级"这种兜底措辞 |
| 4 | 转正/保留/排除判定门槛（第八节） | 部分已修正，部分待补充 | 已补上"类型退化不得自动转正"和"失败率必须人工判定"两条规则；平均分2.5/1.5分界、0分占比20%、提升20个百分点、标准差3%这几个具体数字仍是预设值，待你确认 |
| 5 | 重复次数 | 按预设值执行 | 最低3次成功结果，暂不提高 |
| 6 | 参考蒙版标注工具 | 按预设值执行 | 不指定具体软件，固定输出格式（255/0二值、单通道）即可 |
| 7 | 蒙版复核重叠率门槛（98%） | 按预设值执行 | 作为设计阈值使用，报告中需注明这是设计阈值而非实测校准值 |
| 8 | 耗时测量设备 | 待补充 | 测试前必须固定并记录到"运行环境记录表"，本文件不代替你选定具体设备 |
| 9 | 失败上限 | **已修正** | 改为"失败尝试总次数最多5次"（不是补跑次数），且与预算上限构成双重停止条件，任一先满足即停止（见第三节） |
| 10 | 双人标注 | **已修正** | 双人独立标注优先，单人间隔复标为明确降级方案，不构成测试启动的硬阻塞（见第四节） |
| 11 | 算法辅助标注（路径B）三个阈值：色度距离背景上限40/前景下限140/核心腐蚀半径6像素 | 待补充（新增项） | `generate-reference-mask.ts`当前默认值，只用合成测试图验证过机制正确性，未在真实HAIR/LEAF生成图上校准，需要你看到真实素材的模糊区域占比后再确认是否合适 |

**本轮新增的一项（原第八节的缺口）**：失败率没有预设数值门槛，改为"决策时必须列出实际失败率和失败原因，由用户人工判定可接受/有条件可接受/不可接受"，这个判定方式本身已确定，但"是否要在此基础上再加一个具体数值门槛"仍留待你在看到预实验结果后决定。
