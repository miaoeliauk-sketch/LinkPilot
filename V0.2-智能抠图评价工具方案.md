# V0.2 智能抠图独立评价工具方案

> 对应任务书第2项，主状态：`原型验证，待实测`。本方案是工具设计文档，配套骨架代码见 `scripts/matting-evaluation/`（本次一并产出，仅含公式类纯函数，不含实际抠图调用）。
> 边界（必须遵守）：工具**只读取已有的输入图和已有的抠图输出图**做量化比对，**不生成新图片，不调用 Codex/gpt-image-2，不修改 `src/composite.ts`、`src/background.ts`、`src/intensity.ts`、`src/metadata.ts` 或正式 pipeline 的其他文件**。工具产出的所有数字仍需人工填入《V0.2-智能抠图测试设计表.md》，工具本身不直接下"转正/保留/排除"结论。

---

## 一、定位与目录结构

```
scripts/matting-evaluation/
├── README.md              # 使用说明（人工操作步骤，工具不自动化整个流程）
├── types.ts                # 类型定义
├── alpha-classify.ts       # alpha三档判定（纯函数）
├── metrics.ts               # 核心/细节保留率、半透明占比、丢失率、误抠面积等计算（纯函数）
├── mask-io.ts               # 参考蒙版读取、版本号解析
├── env-snapshot.ts          # 运行环境信息采集（Node版本等程序可获取部分；GPU/CPU等需人工确认部分留空提示）
├── report.ts                 # 汇总输出为JSON + Markdown摘要
└── cli.ts                    # 命令行入口，串联以上模块
```

这套目录与 `src/` 完全独立，不 import 任何 `src/*` 模块，也不被 `src/` 引用，避免和正式 pipeline产生耦合。

---

## 二、输入文件格式

- **抠图结果图**：RGBA PNG，色度键路线和智能抠图路线的输出都用同一格式读取（保证两条路线可比）。工具用 `sharp(path).ensureAlpha().raw()` 读取为像素缓冲区，若读取后通道数不为4，直接报错退出（沿用《V0.2-接口兼容性审查报告.md》确认的`composeDecorations`同款错误处理风格：描述性中文错误，非静默失败）。
- **原图**（可选，用于人工比对，不参与量化计算）：不强制要求工具读取，量化指标只依赖抠图结果图和参考蒙版。

## 三、参考蒙版格式

- 与《V0.2-智能抠图测试设计表.md》第四节规范一致：单通道PNG，255=主体、0=背景。
- 每个素材编号对应两份蒙版文件：核心区域蒙版、细节区域蒙版，命名规则见《V0.2-素材蒙版与授权检查规范.md》。
- 工具读取蒙版时校验：①单通道或可转换为单通道；②尺寸与被测图一致（若不一致，报错退出，不自动缩放蒙版去凑配，因为蒙版缩放会引入新的误差来源）；③像素值只能是0或255，若发现中间值（标注工具导出时可能产生抗锯齿灰阶），报错提示需要重新导出二值蒙版，不由本工具自动二值化替标注者做决定。

## 四、Alpha三档判定

严格复用《V0.2-智能抠图测试设计表.md》第五节定义：`alpha≥250`完全不透明、`alpha≤5`完全透明、`6≤alpha≤249`半透明。实现为纯函数 `classifyAlpha(alpha: number): "opaque" | "transparent" | "semi"`，三档判定阈值作为可配置参数（默认值取自测试表，但允许通过命令行覆盖，因为测试表第九节明确这些阈值"按预设值执行，正式测试前需再次确认"，工具需要支持后续调整而不必改代码）。

## 五、各项计算

以下全部实现为独立可单测的纯函数，输入是像素级分类结果（蒙版区域 × alpha分类），输出是数字，公式与《V0.2-智能抠图测试设计表.md》第六节逐字对应：

| 函数 | 对应指标 | 公式来源 |
|---|---|---|
| `coreRetentionRate` | 核心保留率 | 测试表六节 |
| `detailOpaqueRetentionRate` | 细节完全不透明保留率 | 测试表六节（已改名后的正式字段名） |
| `detailSemiTransparentRatio` | 细节半透明占比 | 测试表六节 |
| `detailTransparentLossRate` | 细节完全透明丢失率 | 测试表六节（不是100%−保留率，单独用alpha≤5计算） |
| `obviousFalsePositiveArea` | 明显误抠面积 | 测试表六节 |
| `detailFalsePositiveArea` | 细节误抠面积（含外扩5像素缓冲带，缓冲带宽度可配置） | 测试表六节 |

三个比例类指标（保留率/半透明占比/丢失率）计算后自检 `保留率+半透明占比+丢失率 = 100%`（允许极小浮点误差），若校验失败，工具判定为内部计算错误并报错退出，不静默输出可能有误的数字。

**不在本工具范围内的指标**：边缘白边/污染等级（人工目测评级，测试表已明确要求两人独立评级）、运行时间/内存显存（由运行智能抠图工具本身的调用方记录，不是评价工具的职责）、重复运行稳定性的标准差（由汇总多次工具输出后人工或后续脚本计算，不在单次评价工具的职责内）。

## 六、蒙版版本记录

工具从蒙版文件名解析版本号（命名规则见素材蒙版规范文档，如 `HAIR-01-core-v1.png`），若文件名不含版本号，报错提示按命名规则重新命名，不允许工具"猜测"或默认赋值v1，避免版本追溯出错。

## 七、输出 JSON 字段

```json
{
  "素材编号": "",
  "路线": "色度键 | 智能抠图",
  "运行序号": "",
  "核心保留率": null,
  "细节完全不透明保留率": null,
  "细节半透明占比": null,
  "细节完全透明丢失率": null,
  "明显误抠面积": { "像素数": null, "占比": null },
  "细节误抠面积": { "像素数": null, "占比": null, "缓冲带宽度像素": 5 },
  "蒙版版本": { "核心区域": "", "细节区域": "" },
  "alpha阈值参数": { "不透明下限": 250, "透明上限": 5 },
  "脚本版本": "",
  "计算时间戳": "",
  "输入文件路径": { "抠图结果图": "", "核心蒙版": "", "细节蒙版": "" }
}
```

字段命名与《V0.2-智能抠图测试设计表.md》第八节决策项表完全一致，方便直接复制填表，不需要人工做字段名转换（转换过程本身容易出错、遗漏）。

## 八、输出 Markdown 摘要

工具在输出JSON的同时，生成一份人类可读的Markdown片段（表格形式，字段顺序与测试表第三节对照记录表列顺序一致），供直接粘贴进测试表对应行，减少手工誊抄导致的数字错位风险。摘要末尾固定加一行提醒：

> 以上数字由 `scripts/matting-evaluation` 工具计算产出，仅供填表参考；边缘污染等级、运行时间、内存/显存、重复运行稳定性等需人工评级或另行记录的字段本工具不产出，需按测试表要求单独填写。

## 九、运行环境记录

工具自动采集程序可获取的部分（`node -v`、脚本自身版本、运行时间戳），写入输出JSON的`运行环境`字段；CPU型号、内存容量、GPU型号、显存容量等无法通过Node.js标准API可靠跨平台获取的字段，工具**不猜测、不留空静默**，而是在命令行输出中明确提示"以下字段需人工补充：CPU型号/内存容量/GPU型号/显存容量"，强制使用者去测试表的"运行环境记录表"里手动填写，避免自动化程度不足反而误导为"已记录"。

## 十、脚本版本

`scripts/matting-evaluation/` 目录内维护一个 `VERSION` 常量（如 `"0.1.0-draft"`），每次公式或阈值默认值发生变更时递增，写入每次输出的JSON，确保后续复查能追溯"这次测量用的是哪个版本的工具"，避免测试表第九节提到的"新旧口径混用"问题在工具层面重演。

## 十一、参数快照

每次运行把实际使用的参数（alpha三档阈值、外扩缓冲带宽度像素、蒙版路径等）完整写入输出JSON的`alpha阈值参数`等字段（见第七节），不只依赖代码里的默认值——如果使用者通过命令行覆盖了默认阈值，输出必须体现实际用的值，不能让读者误以为用的是文档默认值。

## 十二、测量失败时的错误处理

延续《V0.2-接口兼容性审查报告.md》第四节确认的项目既有风格：

- 参数缺失/文件不存在/格式不对/尺寸不匹配/蒙版含非二值像素/比例自检不等于100% 等情况，**一律直接抛出描述性中文错误信息并以非零退出码结束**，不静默跳过、不输出"部分结果"、不用默认值顶替缺失数据。
- 错误信息需包含具体文件路径和具体原因，例如"核心蒙版尺寸(800×600)与抠图结果图尺寸(1024×1024)不一致：/path/to/mask.png"，不是笼统的"计算失败"。
- 工具运行中途失败，不产出JSON/Markdown输出文件（避免半成品输出被误当作完整结果使用）。

---

## 十三、明确不做的事（避免范围蔓延）

- 不实现智能抠图本身（工具只评价已有的抠图结果，不负责生成抠图结果）。
- 不实现色度键抠图本身（同上）。
- 不自动化"边缘污染等级"评分（必须人工目测，测试表已有明确的两人独立评级要求）。
- 不自动计算重复运行稳定性的标准差（这是跨多次运行的统计量，建议放在测试表的"汇总统计栏"人工或用另一个小脚本汇总，不塞进单次评价工具里增加复杂度）。
- 不写入 `src/metadata.ts` 的 `GenerationRecord`（智能抠图仍是独立原型验证阶段，尚未接入正式记录机制；接入是任务卡3决策"转正"之后才需要讨论的事）。
