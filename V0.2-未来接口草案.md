# V0.2 未来接口草案（非冻结）

> **这只是草案，不是已经冻结的正式接口。** 本文件不修改现有代码，不开始开发正式界面，只是把未来界面/调用方可能需要的字段先记录下来，供后续设计讨论使用。正式用户界面尚未进入当前V0.2开发范围。

---

## 一、定位

延续《第0步-FramePilot与My-Motion技术栈扫描结果.md》"V0.2 对接建议"的方向：本机HTTP接口，`multipart/form-data`上传，返回文件路径而非Base64。本草案只列字段，不涉及路由设计、鉴权、并发处理等实现细节，那些属于真正开发界面时才需要讨论的问题。

---

## 二、输入字段（草案）

| 字段 | 类型 | 说明（草案，未冻结） |
|---|---|---|
| `image` | 文件 | 待处理的原始图片（multipart上传） |
| `intensity` | 字符串枚举 | `light` / `standard` / `rich`，对应现有`src/intensity.ts`的三档强度 |
| `segmentationMode` | 字符串枚举 | 草案值：`chroma-key`（现有色度键路线）/ `ai-matting`（若智能抠图转正后可选） —— **`ai-matting`选项当前不可用，智能抠图仍是原型验证阶段，此字段列出是为了未来预留，不代表现在已支持** |
| `outputSize` | 数字或字符串 | 草案默认`1024`（对应现有pipeline统一输出1024×1024约定），是否需要支持其他尺寸待讨论 |
| `requestId` | 字符串 | 调用方生成的请求追踪ID，用于日志和结果查询关联，具体生成规则（UUID/自增等）待讨论 |

## 三、输出字段（草案）

| 字段 | 类型 | 说明（草案，未冻结） |
|---|---|---|
| `outputPath` | 字符串 | 结果文件的本地路径（沿用第0步扫描结论的"返回路径而非Base64"原则） |
| `mimeType` | 字符串 | 草案固定`image/png`（现有pipeline输出格式） |
| `width` / `height` | 数字 | 实际输出尺寸 |
| `metadata` | 对象 | 草案：复用或参考现有`GenerationRecord`结构（`input_image`/`source_type`/`params`/`output_image`/`timestamp`/`human_review`），具体是否直接复用还是另设精简版本待讨论 |
| `processingMode` | 字符串枚举 | 与输入的`segmentationMode`对应，记录实际使用的处理路线 |
| `status` | 字符串枚举 | 草案值：`success` / `failed` / `partial`（"partial"用于类似"生成成功但背景校验重试后仍不理想"这类现有pipeline已有的场景） |
| `failureType` | 字符串枚举 | 草案：复用《V0.2-错误分类与错误映射.md》的8类失败分类（素材问题/工具崩溃/超时/输出损坏/输出不可用/依赖缺失/内存不足/其他），`status`为`success`时此字段为空 |

---

## 四、与现有V0.1接口的关系（草案层面的观察，非结论）

- 现有V0.1是纯CLI（`pnpm compose`/`pnpm background`/`pnpm intensity`/`pnpm record`），本草案描述的是未来HTTP化之后的字段形态，两者字段不是一一对应，需要一层适配（例如CLI的`--source-type`对应`GenerationRecord.source_type`，HTTP接口草案里暂未单独列出，是否需要作为输入字段之一待讨论）。
- `segmentationMode: "ai-matting"`这个草案选项的存在，不代表智能抠图已被批准接入正式流程——这只是草案里为未来可能性预留的占位，实际是否加入需要等任务卡3的转正决策，且即使转正，具体接口形态也可能与本草案不同。

---

## 五、明确声明

- 本文件不接入任何现有代码，不修改`src/`。
- 本文件列出的字段是讨论起点，不是需求冻结，字段名、类型、枚举值都可能在实际设计时调整。
- 不会仅因为本文件存在就开始开发正式用户界面或HTTP服务。
