# V0.2 浓郁档飞白/白斑测量规范

> 对应任务书第3项，主状态：`待统一口径复测`（固定文字，与《国风手账素材工具_V0.2开发任务书.md》保持一致）。本规范文件属于该项"当前可执行部分"中"已确认，可立即推进"的工具准备子任务，规范本身完成不代表主状态解除——只有用本规范复测出可信数字后，主状态才可能变化。
> 目的：在复测"浓郁档白斑占比10.04%→1.82%"这一待核对结论之前，先把测量口径固定下来，确保这次测量和 Day6/Day7 的测量、以及未来任何一次复测，用的是同一套规则，数字才能互相比较。
> 本文件只定义规范，不填写任何具体测量结果，也不确认摘要里的10.04%/1.82%已经成立。

---

## 一、有效像素区域定义

- 测量范围仅限主体的透明PNG中 alpha > 0 的像素（即实际主体像素），不包括背景/装饰层。
- 若装饰元素（印章/云纹/胶带）已叠加到同一张图上，需要先排除装饰元素占用的像素范围，避免把装饰色误判为"白斑"。判断方式：使用叠加装饰前的原始主体透明图做测量，不用最终成品图。
- 若主体存在部分透明的抗锯齿边缘（alpha在1-254之间的过渡像素），本规范要求**排除**边缘过渡带（例如向内收缩1像素，与Day3抠图收缩参数保持一致的收缩量，避免引入新的口径变量），只统计 alpha≈255 的完全不透明区域。

## 二、白斑判定阈值

- 判定目标：识别主体像素中"应为浓郁水墨/工笔表现，实际呈现为大片单调浅色平涂或做旧磨损感"的区域，而不是所有浅色像素。
- 判定条件需要同时满足（避免把正常的月白色板色块或合理留白误判为白斑）：
  1. 像素颜色在RGB空间中与色板"月白 #F5F0E8"的欧氏距离小于既定阈值（阈值数值待确定，需要用已有的Day6/Day7素材反推校准，不在本规范中臆定具体数字）
  2. 该像素所在的连通区域面积超过最小连通区域阈值（避免把单个像素噪点计入统计）
  3. 该像素不属于色板中其他四色（朱红/黛蓝/藤黄/赭石）的合理阴影/高光过渡范围
- 阈值数值本身需要专门一步"口径校准"：用Day6/Day7报告中已经人工确认过的白斑区域样本反推出阈值，而不是凭空定义，确保新脚本复现旧结论时数字一致或可解释差异。

## 三、透明边缘处理

- 明确排除透明边缘过渡带（见第一节），避免边缘半透明的浅色抗锯齿像素被误判为白斑，人为拉高占比。
- 排除方式与Day3抠图的"收缩1像素"操作保持术语和量级一致，但这是两个不同的处理步骤（抠图收缩 vs 测量收缩），需要在报告中明确区分，不要混用同一次收缩结果。

## 四、图像尺寸与缩放方式

- 测量必须在统一分辨率下进行：使用pipeline最终输出尺寸（1024×1024，参照任务书流程图），而不是模型原始输出尺寸（gpt-image-2常见约1254×1254）。
- 若测量脚本需要缩放图像，必须使用与生产pipeline相同的缩放算法（当前使用sharp库的resize，具体参数需在脚本实现时注明：插值方式、是否保持长宽比），避免缩放算法差异引入统计噪声。

## 五、色彩空间

- 统一使用sRGB色彩空间做像素比较，与DESIGN.md色板hex值定义的色彩空间保持一致。
- 若测量脚本内部转换到其他色彩空间（如Lab，用于更准确的"色彩距离"计算），需要在报告中注明转换方式和转换库，不能默认读者知道用了什么色彩空间。

## 六、白斑占比计算方式

- 占比 = 判定为白斑的像素数 / 有效像素区域总像素数（第一节定义的范围），不是占整张图（含背景/透明区域）的比例。
- 需要同时报告绝对像素数和占比百分比，避免不同图片分辨率导致占比无法直接比较绝对严重程度。

## 七、连通区域统计方式

- 使用连通区域分析（4-连通或8-连通需在报告中注明选择哪种，建议8-连通，因为斜向相邻的白斑像素在视觉上仍是连成一片的做旧感）。
- 需要报告：白斑总面积占比（前述第六节）+ 最大单个连通区域面积占比（用于区分"大片明显白斑"和"很多分散小白点"两种视觉观感完全不同的情况，即使总占比相同）。

## 八、与Day6、Day7数据的兼容性

- Day6/Day7报告中已有的白斑相关数字（如10.04%基准），需要用本规范重新计算一次作为基线复现测试：如果重新计算得到的数字与Day6/Day7报告中的数字明显不同，说明本规范定义的口径与当时使用的口径不一致，需要在报告中明确写出差异，而不是直接采用本规范数字替换旧数字。
- 只有基线复现测试通过（即用本规范重新算Day6/Day7的样本，得到和原报告一致或可解释的数字），才能认为本规范"向后兼容"，新测的1.82%这类数字才能和10.04%放在同一张表里比较。

## 九、口径不一致时的处理方式

若发现口径不一致（例如阈值定义不同、有效区域定义不同）：

1. 不强行"统一换算"（即不能凭猜测把旧口径的10.04%换算成新口径下的等效值）。
2. 用新口径对旧素材（若旧素材文件仍然可获取）重新测量一次，得到新口径下的旧数字，再和新口径下的新数字比较。
3. 若旧素材已不可获取，如实记录"两次测量口径不可比，10.04%和1.82%不能直接得出改善结论"，作为待核对状态保留，不强行给出"已优化"的结论。

---

## 十、脚本实现待办（不在本文件中实现，仅列出要求）

- 语言/环境：建议与现有pipeline保持一致（Node.js + TypeScript + sharp），避免引入新的图像处理依赖，除非现有库无法满足连通区域分析需求。
- 输入：主体透明PNG（装饰叠加前的版本）。
- 输出：JSON格式的测量结果，字段至少包含：`有效像素总数`、`白斑像素数`、`白斑占比`、`最大连通区域占比`、`阈值参数`、`色彩空间`、`测量脚本版本`。
- 测量结果建议直接写入现有 `metadata.ts` 的 `params` 字段（Record<string,unknown>类型，可扩展），保持和现有生成记录同一套数据留存机制，不另起一套存储格式。
