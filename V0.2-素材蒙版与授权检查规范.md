# V0.2 素材、蒙版与授权检查规范

> 用途：统一V0.2各测试（智能抠图、回归测试等）共用的素材编号、蒙版命名、授权状态枚举、提交前检查规则，避免各文档各自定义一套导致口径不一致。本文件只定义规则，**不写入任何真实人物姓名、联系方式或照片内容**。
> 涉及文件：《V0.2-智能抠图测试设计表.md》《V0.2-回归测试矩阵与授权记录模板.md》《V0.2-Codex任务卡集合.md》任务卡3/5均应遵循本规范，具体表格结构仍以各自文件为准，本文件是共用规则的单一来源。

---

## 一、素材编号规则

- 格式：`<类型前缀>-<两位序号>`，例如 `HAIR-01`、`LEAF-02`、`PERSON-A`、`OBJECT-CERAMIC-01`。
- 类型前缀固定枚举：
  - `HAIR`：发丝类（智能抠图测试）
  - `LEAF`：细叶类（智能抠图测试）
  - `PERSON-<字母>`：真人回归测试对象（用字母而非姓名区分，如 `PERSON-A`、`PERSON-B`）
  - `OBJECT-<材质类型>`：常见物件回归测试对象，材质类型用英文大写（`CERAMIC`陶瓷/`WOOD`木质/`FABRIC`布艺/`METAL`金属）
- 序号从01开始，两位数字，不复用已废弃编号（即使某个01素材因授权问题被排除，02依然从02开始，不回收01给新素材，避免历史记录引用混淆）。
- 编号本身不含任何可识别真实身份的信息（不用姓名缩写、生日、住址等作为编号的一部分）。

## 二、核心蒙版和细节蒙版命名规则

- 格式：`<素材编号>-<区域类型>-v<版本号>.png`
- 区域类型固定枚举：`core`（核心区域蒙版）、`detail`（细节区域蒙版）
- 版本号：整数，从1开始，标注通过复核后才允许写入正式版本号（草稿阶段可用 `-draft` 后缀区分，如 `HAIR-01-core-draft.png`，草稿文件不得用于第三节正式对照测试）
- 示例：`HAIR-01-core-v1.png`、`HAIR-01-detail-v2.png`（细节蒙版因发现标注错误升级到v2，核心蒙版未变仍是v1）
- 版本号只增不减，修正蒙版必须新建文件+新版本号，**不得覆盖旧版本号对应的文件**（旧文件保留，便于追溯历史结果用的是哪个版本）。

## 三、蒙版版本号规则（补充说明）

- 每次蒙版内容发生任何改动（不论多小），版本号必须递增，不允许"内容变了但文件名/版本号不变"的情况——这是《V0.2-智能抠图测试设计表.md》第四节"冻结规则"和《V0.2-智能抠图评价工具方案.md》"蒙版版本记录"章节共同要求的硬约束，工具（`scripts/matting-evaluation`）会从文件名解析版本号，文件名不含版本号或版本号未变但内容变了，会导致历史结果无法正确追溯。
- 版本变更需要在测试记录（测试表第三节或素材台账）中同步注明变更原因和变更时间，不是只改文件名了事。

## 四、输出文件命名规则

- 抠图结果输出：`<素材编号>-<路线>-run<运行序号>.png`
  - 路线固定枚举：`chroma`（色度键）、`matting`（智能抠图）
  - 示例：`HAIR-01-matting-run1.png`、`HAIR-01-matting-run2.png`（同素材同路线第2次运行）
  - 失败运行也要保留对应文件（如果有部分输出）或至少保留一条失败记录（见《V0.2-智能抠图测试设计表.md》第三节"失败运行的统计方式"），文件名序号与测试表记录表的"运行序号"列必须一一对应，不得错位。
- 评价工具输出：`<素材编号>-<路线>-run<运行序号>-eval.json` / `.md`，与抠图结果文件同素材同路线同序号一一对应。

## 五、授权状态枚举（固定，跨文档统一）

| 允许值 | 含义 | 是否可进入测试 |
|---|---|---|
| `已授权` | 真人素材，已获得本人书面或口头同意（需另行记录确认方式，但确认方式本身不写入会提交到仓库的文件），或用户自行拍摄的非真人素材 | 可以 |
| `AI测试素材` | 非真人素材，或AI生成的测试素材，不涉及真人授权问题 | 可以 |
| `未授权-不可用` | 授权状态未确认，或已知未获得授权 | **不可以** |

- 空值、"待定"、"可能可以"、"应该没问题"等任何非上述三个精确枚举值的表述，**一律视同 `未授权-不可用`**，不放行。
- 本枚举与《V0.2-智能抠图测试设计表.md》第一节、《V0.2-回归测试矩阵与授权记录模板.md》第四节使用的枚举逐字一致，任一文档不得自行发明新的授权状态值。

## 六、测试前授权检查清单

在任何素材进入第三节/第三步实际对照测试之前，逐项确认：

- [ ] 该素材的授权状态字段已填写，且值为`已授权`或`AI测试素材`（不是空值/待定/未授权-不可用）
- [ ] 若为`已授权`的真人素材，授权确认方式已在本地记录（不提交到公开仓库），且授权人明确知晓素材用途（V0.2抠图/风格化测试）
- [ ] 该素材的本地保存路径已记录，且路径不在仓库的Git跟踪范围内（见下节`.gitignore`检查）
- [ ] 若同一素材需要多次使用（例如既用于智能抠图测试又用于回归测试），两处记录的授权状态一致，不得一处写已授权、另一处遗漏或写别的值

## 七、`.gitignore` 与 `git status` 提交前检查项

- 仓库根目录 `.gitignore` 需要包含真人素材可能存放的本地目录（本文件第九节规定的目录结构），例如：
  ```
  /local-assets/
  /local-assets/**
  *.local.png
  ```
  具体规则以实际仓库 `.gitignore` 内容为准，本文件只规定"必须存在覆盖本地素材目录的规则"这一要求，不代替读取/修改实际 `.gitignore` 文件（属于任务卡执行时的动作，不在文档准备阶段直接改）。
- 每次向仓库提交内容前，固定执行：
  1. `git status` 目视检查，确认待提交文件列表中不包含任何真人素材文件、蒙版文件（若蒙版由真人照片标注而来）、或含真实素材路径的记录文件；
  2. `git diff --stat` 复核变更文件列表和改动量级是否符合预期（避免不小心把大图片误加进提交）；
  3. 如发现真人素材文件出现在待提交列表中，**立即从暂存区移除**（`git restore --staged <path>`），不得先提交再补救。
- 这一检查项在《V0.2-回归测试矩阵与授权记录模板.md》第四节已有类似要求，本节是跨所有V0.2测试（含智能抠图）的统一版本，两处要求一致，互不矛盾。

## 八、真人素材只保存在本地的说明

- 真人照片原始文件、基于真人照片标注的蒙版文件、真人照片的抠图/生成结果文件，**一律只保存在本地磁盘，不提交到公开GitHub仓库**，无论是否已获得授权——授权只解决"能不能用这张照片做测试"，不改变"公开仓库不存真人素材"这条独立的数据处理原则。
- 测试记录文档（如测试表、矩阵）可以提交到仓库，但记录中涉及本地路径的字段，写相对/占位路径描述（如"本地素材目录/HAIR-01原图"）即可，不需要、也不应该写出完整的真实本地绝对路径（可能暴露本机用户名等信息）。
- 任何汇总报告、验收结论中引用真人测试结果时，只引用编号（如`PERSON-A`）和量化数据，不附带照片本身或可识别外貌特征的描述。

## 九、失败样本和结果文件的本地目录规范

建议的本地目录结构（不要求强制照搬，但结构划分逻辑需要保持：原始素材、蒙版、结果、失败记录四类分开存放，不混在一个文件夹）：

```
local-assets/                      # 整个目录已在 .gitignore 中排除
├── raw/                            # 原始素材（真人照片、AI测试素材）
│   ├── HAIR-01.jpg
│   └── ...
├── masks/                          # 参考蒙版（含草稿和已冻结版本）
│   ├── HAIR-01-core-v1.png
│   ├── HAIR-01-detail-v1.png
│   └── ...
├── results/                        # 抠图/生成结果（成功）
│   ├── HAIR-01-matting-run1.png
│   └── ...
├── failed/                         # 失败运行的产出（若有部分输出文件）
│   ├── HAIR-01-matting-run2-failed.png   # 若失败时仍产出了（哪怕损坏的）文件
│   └── HAIR-01-matting-run2-failed.log    # 失败原因日志
└── eval/                            # scripts/matting-evaluation 工具输出
    ├── HAIR-01-matting-run1-eval.json
    └── HAIR-01-matting-run1-eval.md
```

- `failed/` 目录存在的意义：呼应《V0.2-智能抠图测试设计表.md》"失败运行必须保留完整记录，不得删除"的要求——即使输出不可用，只要产生了文件（哪怕是损坏的PNG或空文件），也归档而不是直接删除，方便后续排查失败原因。
- 若某次失败完全没有产出任何文件（例如进程崩溃前就没写文件），至少在`failed/`目录下留一个同名的`.log`文本文件记录失败原因和时间戳，保证"这次尝试发生过"这件事本身可追溯。
