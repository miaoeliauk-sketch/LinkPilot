# V0.2 错误分类与错误映射

> 用途：统一V0.2各测试环节（智能抠图任务卡3、浓郁档复测任务卡2、回归测试任务卡5等）遇到失败运行时使用的分类体系，明确每类失败对统计口径（失败率/预算/补跑/验收）的影响，以及未来正式界面可以直接复用的提示文案。
> 本文件只做错误模型的文档定义，**不开发正式界面代码**，不修改`src/`。
> 与现有文档的关系：《V0.2-智能抠图测试设计表.md》第三节此前使用"素材问题/工具崩溃/超时/结果不可用/其他"5类分类，本文件把"结果不可用"拆分为"输出损坏"和"输出不可用"两类，并新增"依赖缺失""内存不足"两类，是更细的8类版本。**这一细化尚未回填进测试设计表**，属于本轮发现的一个待协调事项，见文末说明，不在本文件内直接改动测试表。

---

## 一、错误分类与映射表

| 分类 | 定义 | 是否计入失败率 | 是否计入预算 | 是否允许补跑 | 是否计入最终验收依据 | 记录字段 | 未来界面可显示的提示文本（草稿） |
|---|---|---|---|---|---|---|---|
| **素材问题** | 输入素材本身不符合要求（格式不支持、分辨率过低、文件损坏无法读取等），非工具/模型问题 | 是 | 是（消耗了一次调用机会） | 是（更换或修复素材后可补跑） | 是（作为失败率的一部分） | 素材路径、具体错误信息、素材问题子类型（格式/分辨率/损坏） | "上传的图片无法处理，请检查文件是否完整或更换其他格式" |
| **工具崩溃** | 抠图工具/进程异常终止（非超时、非内存不足导致的崩溃，如未捕获异常、段错误） | 是 | 是 | 是 | 是 | 崩溃时的错误堆栈或日志片段、进程退出码 | "处理过程中出现异常，已记录错误信息，请重试或联系维护者" |
| **超时** | 单次运行超过预设时间上限仍未完成，被强制终止 | 是 | 是 | 是（但需要在记录中注明连续超时次数，超过一定次数应怀疑是素材或环境问题而非偶发） | 是 | 超时阈值设定值、实际运行时长（若可获取部分进度） | "处理超时，请稍后重试；如反复超时，可能是图片过于复杂" |
| **输出损坏** | 运行"完成"但产出的文件本身损坏（无法被图像库正常解码、文件截断等） | 是 | 是 | 是 | 是 | 文件路径、文件大小、解码报错信息 | "生成结果文件异常，请重试" |
| **输出不可用** | 文件本身可以正常打开，但内容明显不符合预期（如完全透明、纯黑、与输入毫无关系），不是"损坏"而是"结果不对" | 是 | 是 | 是 | 是（且应额外标注"是否值得纳入0分结果评分"而非只算失败——见下方"输出不可用"与"0分结果"的区分说明） | 文件路径、人工确认"不可用"的具体理由 | "生成结果不符合预期，请重试" |
| **依赖缺失** | 运行环境缺少必需的库/模型文件/权重文件，导致工具无法启动或执行到一半报错找不到依赖 | 视情况——若是环境配置问题非工具本身缺陷，**建议不计入失败率**（因为这反映的是环境搭建问题，不是抠图方案本身的质量），但需要单独统计"环境问题次数"，不能既不计入失败率又完全不记录 | 视情况：若尚未真正开始处理就报错（例如启动阶段就发现依赖缺失），建议**不计入预算**；若已消耗部分调用/计算资源后才报错，计入预算 | 是（修复环境后重跑，且严格说不算"同一次尝试的补跑"，是环境修好之后的正常重跑） | 否，不作为智能抠图方案本身质量的失败率依据，但需要在决策依据中单独说明"测试过程中出现过N次环境依赖问题"，供判断测试本身是否可靠 | 缺失的具体依赖名称/版本、修复方式 | （不面向最终用户，属于内部运维问题，不需要用户可见提示文本；如需开发者可见日志，用"缺少依赖：{name}，请检查安装"） |
| **内存不足** | 运行过程中因内存/显存耗尽被系统或工具终止 | 是（这是工具在给定素材/环境下的真实表现，应计入失败率，除非能证明是测试设备配置明显低于目标运行环境） | 是 | 是（但需要在记录中注明，若同一素材反复内存不足，应作为"该素材在当前资源限制下不适用"的证据之一，而非无限重试） | 是 | 峰值内存/显存监控数值（若能捕获到终止前的读数）、设备规格 | "处理所需资源超出限制，建议尝试更小尺寸的图片" |
| **其他** | 以上7类均不适用的失败情况 | 是 | 是 | 是 | 是 | 尽可能详细的原始错误信息，且要求在记录中说明"为何不属于以上7类"，避免"其他"被滥用成不分类的垃圾桶 | "处理失败，请重试；如持续出现，请联系维护者" |

---

## 二、"输出不可用"与"0分结果"的区分说明（重要，避免和评分体系混淆）

《V0.2-智能抠图测试设计表.md》里的"0分结果"是**成功运行、但质量评分为0分**（例如核心保留率<95%），前提是这次运行本身**没有被判定为失败**——评价工具能正常读到一个输出文件并计算出指标。

而本文件的"输出不可用"是**运行虽然产出了文件，但内容本身荒谬到无法进行有意义的评价**（比如完全透明、和输入完全不相关的图案）——这种情况下不应该硬套评分公式算出一个可能没有意义的0分，而应该先判定为"输出不可用"这一类**失败**，不进入评分流程。

两者的分界线（判断顺序）：

1. 文件能否正常解码？不能 → **输出损坏**（失败）。
2. 能解码，但内容是否明显荒谬（完全透明/纯色/与输入无关）？是 → **输出不可用**（失败，不进入评分）。
3. 能解码且内容大致合理（有主体轮廓，只是质量不够好）→ 进入正常评价流程，可能算出0分（**这是"0分结果"，不是"失败"**）。

第2、3步的判断目前依赖人工目测，本文件不提供自动化判定规则（涉及主观判断，强行自动化容易引入新的争议点，此处明确留给人工，不在错误模型文档里假装能自动区分）。

---

## 三、与失败率/预算/补跑/验收的关系（汇总原则）

- **失败率分母**（沿用《V0.2-智能抠图测试设计表.md》第八节定义）：全部尝试次数，含全部8类失败+成功。**"依赖缺失"是否计入失败率分子，按上表标注的"视情况"处理，需要在决策依据中单独说明处理方式**，不能默认计入或默认不计入而不解释。
- **预算**：除"依赖缺失"在特定情况下可不计入外，其余7类均计入预算——预算约束的是"实际消耗的调用/计算资源"，不是"这次尝试是否有价值"。
- **补跑**：全部8类均允许补跑，但补跑仍受《V0.2-智能抠图测试设计表.md》"失败尝试总次数最多5次，或预算上限，任一先满足即停止"的双重停止条件约束，不因为失败类型不同而豁免这个上限。
- **最终验收依据**：除"依赖缺失"外均计入，原因见上表——依赖缺失反映的是本次测试环境配置的问题，不直接反映智能抠图方案本身的可用性，混进去会让"转正/保留/排除"决策失真。

---

## 四、未来界面提示文本的定位说明

上表"未来界面可显示的提示文本"列是**草稿**，用于给未来正式用户界面开发时提供参考起点，本文件产出时**不接入任何界面代码**，不创建界面组件，措辞后续可能需要根据实际产品语气调整，不视为最终文案定稿。

---

## 五、待协调事项（本文件产出时发现，不在本文件内直接处理）

《V0.2-智能抠图测试设计表.md》第三节当前仍使用"素材问题/工具崩溃/超时/结果不可用/其他"5类失败分类（"结果不可用"未拆分，也没有"依赖缺失""内存不足"两类）。本文件的8类是更细的版本。是否要把测试设计表的失败原因列改成8类、或者保留5类作为简化版并注明"详细分类见本文件"，需要用户决定，本次不擅自改动测试设计表的既有结构。
