# InkPilot（暂定名）· 国风手账素材工具 V0.1 开发任务书

> 项目名待最终确认，候选：InkPilot / 裱匠 / 拓本。文中先用 InkPilot 占位，改名只需替换代码里的项目名即可。

> 交给 Claude Code 前，先完成【第0步：技术栈摸底】+【第0.5步：安装图像生成技能】，再进入正式开发。

---

## 第0步：技术栈摸底（开工前必做）

在 Claude Code 里，对 FramePilot 和 My-Motion 两个现有项目分别执行：

```
请扫描这个项目，输出：
1. 主要语言和框架
2. 依赖的第三方库/API
3. 素材的输入输出格式（图片怎么传进来、怎么存、怎么导出）
4. 是否已有对外暴露的接口（HTTP API / 本地函数调用）
5. 项目当前的运行方式（怎么启动、怎么调试）
```

把两份输出保存下来，作为后续"新工具怎么对接"的依据。**不确定技术栈之前，不要开始写新工具的代码。**

---

## 第0.5步：安装图像生成技能（开工前必做）

图像生成这块不用自己接API，用 Claude Code + Codex CLI 的桥接技能。

**前提条件**
- 系统是 macOS 或 Linux
- 已安装 Codex CLI（`npm i -g @openai/codex`，需 v0.130 以上）
- 已执行 `codex login` 登录（用你的 ChatGPT/Codex 订阅即可，不用单独申请 API Key）

**安装桥接技能**

```bash
git clone https://github.com/JunSeo99/claude-skill-codex-imagegen.git
mkdir -p ~/.claude/skills
ln -s "$(pwd)/claude-skill-codex-imagegen/skill" ~/.claude/skills/codex-imagegen
```

重启 Claude Code，让技能被发现。之后直接在对话里说"生成一张……"就会自动调用。

**⚠️ 必须提前知道的限制：透明PNG**

底层模型 gpt-image-2 **不原生支持透明背景输出**。V0.1 要用以下任一方案兜底：
1. 生成时用纯色（色度键）背景，再用脚本本地抠图去背景（推荐，成本低）
2. 显式请求切换到 gpt-image-1.5（原生支持透明，但生成质量/一致性可能有差异，需要人工确认后再切换）

第一版建议先走方案1，跑通再说。

---

## 定位

一个**独立服务**，不合并进 FramePilot / My-Motion 现有代码库。
对外只暴露一个统一接口：输入一张素材图 + 风格参数，输出国风手账风的透明 PNG + 元数据。
FramePilot、My-Motion 以后作为"调用方"接入，互不绑死。

---

## V0.1 范围（严格控制边界）

### ✅ 只做这些

1. 上传单张图片（JPG/PNG/WebP）
2. 自动识别主体（人物/物件），移除背景
3. 保持主体姿势和结构不变
4. 转换成固定的"国风手绘线稿"风格
5. 提供轻度/标准/浓郁三档国风强度
6. 自动叠加少量国风点缀（印章/云纹等，程序化叠加，不靠AI生成）
7. 生成前后对比图
8. 支持局部重新生成（比如只重绘背景，主体保持不变）
9. 导出透明背景 PNG
10. 保存原图、生成结果、使用的参数（供以后复现和评估）

### ❌ 明确不做（写进任务书是为了防止范围蔓延）

- 视频批量转换
- 自动生成完整海报/排版
- 自动生成文案
- 自动配音、自动剪辑
- 多种差异很大的国风子风格（先只做一种，跑通再说）
- 训练专属模型（LoRA等）
- 会员系统、团队协作、移动端

---

## 技术方案（V0.1版本，图像生成用 Claude Code + Codex imagegen 桥接技能）

### 整体流程

```
上传图片
  ↓
【程序处理】背景移除 + 主体检测（用现成的抠图API或库，不需要AI模型）
  ↓
【Codex imagegen 技能】通过 Claude Code 自然语言调用，传入：
   - 原图（已抠图的主体）
   - 固定的五段式风格提示词模板（见下方"风格参数"，参照 DESIGN.md）
   - 强度参数（轻度/标准/浓郁 → 对应不同的 prompt 措辞）
   - 纯色可去除背景（为后续本地抠图做准备，规避 gpt-image-2 不支持透明的限制）
  ↓
【程序处理】本地脚本抠图去背景，叠加国风装饰元素（印章/云纹/胶带 —— 预先设计好的固定PNG素材，程序化贴图，不是AI生成，保证稳定不跑偏）
  ↓
【程序处理】统一缩放到目标输出尺寸（例如1024×1024；gpt-image-2常见实际输出为1254×1254，不能依赖模型严格遵守请求尺寸）
  ↓
导出透明 PNG + 保存元数据（JSON：原图路径、参数、生成时间、模型版本）
```

### 关键原则（来自最初的方案讨论，V0.1必须遵守）

- **文字不交给图像模型生成**——V0.1 不涉及文字排版，此原则留给未来版本
- **装饰元素（印章/云纹/胶带）用程序叠加固定素材，不是每次让AI画**——这是保证"风格一致性"的关键，图像模型只负责"主体重绘"这一件事
- **主体结构必须保留**——用抠图+保留原图轮廓的方式约束，不能让图像模型随意改构图
- **提示词必须用五段式结构**（场景/主体/细节/用途/限制），不要用"国风手账风"这种模糊短语——技能自带的 `prompting-guide.md` 会参考这个结构，同时结合项目根目录的 `DESIGN.md`

### 图像生成技能选型（V0.1阶段，已确定）

用 `claude-skill-codex-imagegen` 桥接 Codex CLI 的 `$imagegen`（gpt-image-2）。开工前按"第0.5步"完成安装。

验证顺序：
- 先跑通"单张图→重绘→拿到结果"最简单闭环
- 测试同一张图跑10次，画风一致性够不够（这是最容易卡住的环节，见下方Day4）
- 测试人物特征保留程度（脸部、姿势是否被改变）
- 关注延迟（复杂提示词可能到2分钟）和成本（按ChatGPT/Codex订阅额度或API Key按量计费，量大时API Key更划算）

如果这条路线效果不稳定，再考虑 ComfyUI 或直接调用 Image API 切到 gpt-image-1.5（属于V0.2以后的事）。

---

## 风格参数（V0.1固定版本，先只做一套）

> 这套参数会同步写进项目根目录的 `DESIGN.md`（见配套文件），Claude Code 每次生成前都会读取这份文件保证风格统一，不用每次在对话里重新描述。

```yaml
国风强度:
  轻度: 保留原图大部分写实感，仅调整配色和轻微线条化
  标准: 明确的手绘线稿+国风配色，装饰元素适中
  浓郁: 强烈水墨/工笔感，装饰元素丰富

固定色板:
  - 朱红 #C23531
  - 黛蓝 #2E4057
  - 月白 #F5F0E8
  - 藤黄 #E8B004
  - 赭石 #A0522D

固定禁止特征（避免AI生成时跑偏）:
  - 不要欧美卡通风
  - 不要日漫风
  - 不要过度写实/照片级
  - 不要出现非中式的建筑/服饰元素
```

---

## 数据留存要求（不能只存最终图）

每次生成必须保存以下内容，供以后复现和优化：

```json
{
  "input_image": "原图路径",
  "params": {
    "国风强度": "标准",
    "模型版本": "xxx",
    "prompt": "实际使用的完整提示词"
  },
  "output_image": "结果图路径",
  "timestamp": "生成时间",
  "human_review": "通过 / 不通过 / 待定（先留空字段，V0.1先不做评分系统，但字段要预留）"
}
```

---

## 验收标准（V0.1完成的判断标准）

- [ ] 能上传一张人物/物件照片，跑完整流程，得到一张透明背景的国风手账风PNG
- [ ] 同一张图，跑3次，风格基本一致（不要求100%一致，但主体不能面目全非）
- [ ] 三档强度效果有可辨识的差异
- [ ] 局部重新生成功能可用（改背景不影响主体）
- [ ] 所有生成记录都有保存，可以在文件夹里翻到对应的原图+参数+结果

---

## 建议的第一周任务拆分

| 天 | 任务 |
|---|---|
| Day 0 | 完成"第0步技术栈摸底"+"第0.5步安装 Codex imagegen 桥接技能"，确定新项目用什么语言/框架 |
| Day 1 | 写好 `DESIGN.md`，跑通"上传图片→Claude Code调用imagegen重绘→拿到结果"最简单闭环，不管美观度，先通 |
| Day 2 | 加上背景移除（抠图，处理gpt-image-2不支持透明的问题） |
| Day 3 | 测试并固定风格提示词模板，跑至少10张不同类型的图看稳定性 |
| Day 4 | 加装饰元素程序化叠加 |
| Day 5 | 加三档强度参数 + 局部重新生成 |
| Day 6 | 数据留存 + 整体验收测试 |
| Day 7 | 缓冲日，处理前面卡住的问题 |

如果Day 3测试发现风格一致性很差（同一张图跑几次差异巨大），先暂停往下加功能，专门解决一致性问题——这是V0.1最容易卡住的环节，Day 7的缓冲日也是为这个问题预留的。
