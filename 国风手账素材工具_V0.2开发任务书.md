# InkPilot（暂定名）· 国风手账素材工具 V0.2 开发任务书

> 命名说明：GitHub仓库及工程代号为`LinkPilot`；当前产品暂定名为`InkPilot`，最终名称尚未确定，候选包括`InkPilot`、`裱匠`、`拓本`。产品名定稿前不调整仓库名、远程地址及相关配置。

> 前置状态：V0.1 已于 commit `6d13103` 正式归档（详见 `V0.1-整体验收结论.md`）。首轮 V0.2 结构化提示词对照实验已执行（13/16次调用），但完整记录 `V0.2-首轮对照实验记录.md` 尚未提供文本供核对，本任务书涉及该实验的结论一律标记"待实验记录核对"，不作为已批准的决定。

---

## 项目边界（本任务书及后续所有 V0.2 工作共同遵守）

1. 不负责新的图片生成——本任务书本身及阶段0-阶段的整理工作不产生新的 Codex 调用。
2. 不根据文字描述伪造视觉实验结果——没有真实图片/记录支持的结论一律写"待实测"或"待核对"。
3. 不宣布结构化提示词已经胜出——即使首轮实验摘要显示胜出，在完整记录核对前维持"待实验记录核对"状态。
4. 不修改正式提示词策略（即不改动 `src/intensity.ts` 中 `buildGenerationPlan` 已上线的五段式模板），除非已有完整实验记录证明并经明确授权。
5. 所有依赖新图片的结论统一标记为"待实测"。
6. 真实人物素材及生成结果仅本地保存，不进入公开仓库。
7. 不推送远程仓库，所有变更只在本地提交，由用户自行确认后推送。

---

## 状态标签说明（两个维度，不要混用）

### 主状态（每项条款的整体定性标签，固定用语）

主状态回答"这一项整体处于什么阶段"，用于任务书、核对清单、任务卡、各测试模板之间互相引用时保持完全一致的文字，不做同义改写。V0.2 五项范围条款固定使用以下五个标签：

| 条款 | 主状态（固定文字，逐字引用） |
|---|---|
| 1. 接入结构化全字段提示词 | `待实验记录核对，暂不批准` |
| 3. 更新浓郁档细线飞白规则 | `待统一口径复测` |
| 2. 发丝、细叶智能抠图原型 | `原型验证，待实测` |
| 4. 玻璃、液体维持排除 | `当前质量保证范围外` |
| 5. 扩大真人和常见物件回归样本 | `可准备，正式验收待实测` |

**主状态不代表"这一项完全不能动手"**——一个条款主状态是"待实验记录核对"，不等于它内部所有子任务都要等；具体哪些子任务现在能做、哪些要等完整实验记录或真实素材，由下面的"当前可执行部分"逐条标注。

### 当前可执行部分（每项条款内部子任务的执行状态）

每一项条款内部拆分成若干子任务，每个子任务用以下五种执行状态之一标注：

| 执行状态 | 含义 |
|---|---|
| **已确认，可立即推进** | 不依赖新图片/新实验数据，可直接开工（如文档整理、测量工具设计、接口审查） |
| **可准备，等待实测** | 测试方案/表格/脚本可以先准备好，但需要真实生成结果或真实素材才能填入结论 |
| **待实验记录核对** | 依赖 `V0.2-首轮对照实验记录.md` 完整文本核对后才能定论，当前不批准执行 |
| **不纳入本版本** | 明确排除在 V0.2 范围外 |
| **延期或保留实验性支持** | 沿用 V0.1 已有的"实验性支持/排除"结论，不重新投入 |

**举例说明两个维度共存、不矛盾**：第3项"浓郁档规则"主状态是`待统一口径复测`（意味着"白斑占比已改善"这个结论现在不能用），但其中"制定统一测量规范"这个子任务的执行状态是"已确认，可立即推进"（这部分现在就能做，且已经做完，见《V0.2-浓郁档测量规范.md》）。主状态描述的是条款的整体结论能否使用，执行状态描述的是具体子任务能否动手。

---

## ⚠️ 待确认事项（本任务书定稿前必须解除）

1. **"结构化提示词胜出"结论**——摘要给出 36:31 分、0 比 2 硬失败，按既定胜出规则逻辑自洽，但完整的 5 维度×每张图评分表尚未核对，不排除加总或记录错误。**主状态：`待实验记录核对，暂不批准`。**
2. **物件A替换为"菠萝屋"摆件**——原计划物件A为"陶瓷杯或木质摆件"，实际测试素材为菠萝造型摆件，替换原因和对可比性的影响尚未在实验记录中确认说明。
3. **相机/三脚架双背景图**——需确认"透明棋盘格背景"输出是否属于 Day3 已知的"假透明棋盘格"失败模式，若是，需明确是否计入 13/16 次调用、评分表如何记分。
4. **浓郁档白斑占比 10.04%→1.82%**——需确认测量脚本/阈值口径与 Day6/Day7 报告是否一致，若不一致需在记录中注明口径变化并重新换算。

---

## V0.2 范围与状态

### 1. 接入结构化全字段提示词 —— 主状态：`待实验记录核对，暂不批准`

- 当前 V0.1 代码中，五段式提示词已直接写死在 `src/intensity.ts` 的 `buildGenerationPlan` 函数内（模板字面量拼接，无独立的"提示词策略"抽象层）。接入结构化全字段格式意味着修改这个函数或新增并行路径——这是一处正式提示词策略变更，按项目边界第4条，必须等完整实验记录核对通过并明确授权后才能执行。
- **当前可执行部分：**
  - 现在可以做的（已确认，可立即推进）：
    - 接口兼容性预评估（见本任务书"接口兼容性"章节及独立报告）：明确新提示词格式必须保持 `GenerationPlan.prompt` 为字符串类型，否则会破坏 `record-cli.ts` 里 `params.prompt` 的写入约定。
    - 为 Codex 准备任务卡（见任务卡集合第4张），但任务卡本身标注"前置条件：实验记录核对通过并获得明确授权"，不允许在前置条件未满足时执行。
- 验收标准：待实验记录核对后，参照阶段0核对结果制定，当前不预设量化标准。

### 2. 发丝、细叶智能抠图原型 —— 主状态：`原型验证，待实测`

- 背景：Day3 结论确认色度键对长发/卷发和细碎轮廓（收缩1像素时约23.5%非零透明像素丢失）存在原理性局限，V0.1 已列为排除范围/实验性支持。
- **当前可执行部分：**
  - 现在可以做的（已确认，可立即推进）：测试设计表、素材编号栏、评价维度、0-3级评价体系（见独立文件《V0.2-智能抠图测试设计表.md》），不填写任何推测数据。
  - 需要等待的（可准备，等待实测）：实际抠图运行、素材采集、评分填写，均需真实素材和真实运行结果。
- 明确要求：**这是原型验证，不默认进入生产流程。** 即使测试结果良好，也需要单独决策是否转正，不因为"测试完成"就自动升级为正式功能。
- 验收标准：见独立测试设计表，本任务书不重复列出。

### 3. 更新浓郁档细线飞白规则 —— 主状态：`待统一口径复测`

- 背景：Day7 结论中浓郁档飞白效果被记录为"看起来像做旧布料而非真正的飞白笔触"，作为非阻塞后续事项遗留。首轮 V0.2 实验摘要称白斑占比从 10.04% 降至 1.82%，但测量口径待确认。
- **当前可执行部分：**
  - 现在可以做的（已确认，可立即推进）：制定统一测量规范/脚本设计（见独立文件《V0.2-浓郁档测量规范.md》），明确有效像素区域、判定阈值、色彩空间等口径，不宣称任何具体数字已经确认。此部分已完成文档产出。
  - 待实验记录核对后才能做的（待实验记录核对）：用统一口径复测现有素材，决定是否更新 DESIGN.md 浓郁档描述及正式提示词措辞（后者同样受项目边界第4条约束）。
- 验收标准：
  - [ ] 测量口径文档化并可复现（待完成，已确认可立即推进）
  - [ ] 用统一口径复测，得到可信数字（待实测）
  - [ ] 浓郁档效果人工目测确认为"飞白笔触感"而非"做旧/磨损感"（待实测）
  - [ ] DESIGN.md 浓郁档描述同步更新（待前几项完成后再执行，且受项目边界第4条约束）

### 4. 玻璃、液体维持排除 —— 主状态：`当前质量保证范围外`

- 背景：Day3 测试证实透明玻璃色度键抠图后出现"空心线框"效果，属于色度键原理性冲突，V0.1 已明确排除。
- 本项主状态`当前质量保证范围外`的含义：不是"以后可能支持但现在没空做"，而是在现有色度键+本地抠图架构下原理性不可行，除非架构发生根本变化（例如智能抠图原型证明可行并转正），否则不重新投入测试或调优。
- **当前可执行部分：** 已确认，可立即推进——本项只需要文档定性说明（把"当前质量保证范围外"这一结论写进面向用户的文档），不需要新实验，因为 V0.1 的 Day3 结论已经充分支撑这个判断。
- 验收标准：
  - [ ] V0.2 范围说明及面向用户的文档明确标注"透明玻璃、液体材质当前质量保证范围外"
  - [ ] 不需要新增测试验证此项（V0.1 的 Day3 结论已充分，重复测试无必要）

### 5. 扩大真人和常见物件回归样本 —— 主状态：`可准备，正式验收待实测`

- 背景：V0.1 最终验收仅基于单一真人测试对象的3次重复生成，首轮 V0.2 实验补充了真人B（3次稳定性验证通过，待记录核对）。样本量仍偏小。
- **当前可执行部分：**
  - 现在可以做的（已确认，可立即推进）：回归测试矩阵模板、授权记录模板（见独立文件《V0.2-回归测试矩阵与授权记录模板.md》），空表结构，不填写任何素材或结果。
  - 需要等待的（可准备，等待实测；正式验收结论需等真实数据）：真实素材采集、真实生成运行、结果评价，均需要真实图片。
- 明确要求：不由 Claude 提供或建议具体真人人选；真人素材仅限已获得本人同意、用户自行拍摄或已获得授权的素材；所有真人素材及生成结果仅本地保存。
- 验收标准：见独立回归测试矩阵文件。

### 6. 接口兼容性（V0.2新增项，来自本轮审查要求）—— 执行状态：**已确认，可立即推进**（不属于上述五项主状态标签体系，本项是独立的代码审查产出，不依赖首轮实验记录）

- 已基于当前仓库远程分支（commit `6d13103`）实际代码完成一轮审查，见独立文件《V0.2-接口兼容性审查报告.md》。
- 结论概要：本轮审查未发现 V0.1 现有接口已被破坏（因为 V0.2 尚未开始改代码），审查产出的是**V0.2 开发过程中必须遵守的约束清单**，供后续每张任务卡引用。
- 验收标准：
  - [ ] 审查报告已产出并覆盖输入/输出字段、RGBA PNG约定、元数据结构、错误处理、装饰流程、风格档位、V0.1回归用例7个方面
  - [ ] 每张 Codex 任务卡的"允许修改的文件"/"禁止修改的文件"栏引用本报告结论

---

## 阶段拆分（含产出物、停止条件、验收状态）

| 阶段 | 内容 | 产出物 | 停止条件 | 对应条款主状态 | 本阶段执行状态 |
|---|---|---|---|---|---|
| 阶段0 | 复核 `V0.2-首轮对照实验记录.md` 完整内容，解除"待确认事项"标记 | 核对清单填写结果、结论标记（可确认/存疑/无法确认） | 拿到完整文本并逐项核对完毕；若记录本身缺关键数据，标记"无法确认"而非强行下结论 | （核对本身不对应单一条款，服务于第1/3项） | 待实验记录核对 |
| 阶段1 | 玻璃液体排除文档化（第4项）+ 浓郁档测量工具准备（第3项工具部分） | 已更新的排除说明、测量规范文档 | 文档产出即完成，不需要新实验 | 第4项：`当前质量保证范围外`；第3项：`待统一口径复测` | 已确认，可立即推进 |
| 阶段2 | 智能抠图原型测试设计与准备（第2项） | 测试设计表、素材编号栏（空表） | 表格结构确认可用，等待实测素材 | `原型验证，待实测` | 已确认，可立即推进（测试设计部分） |
| 阶段3 | 结构化提示词接入（第1项，条件触发） | 视阶段0结论决定：批准则产出实施任务卡执行结果；不批准则产出"维持现状"说明 | 仅在阶段0核对通过且获得明确授权后启动；否则本阶段维持"未启动" | `待实验记录核对，暂不批准` | 待实验记录核对 |
| 阶段4 | 扩大回归样本（第5项） | 回归测试矩阵模板 + 真实测试结果（需要真实素材） | 至少覆盖任务卡设定的最小样本量，或明确记录未达标原因 | `可准备，正式验收待实测` | 已确认，可立即推进（模板部分） |
| 阶段5 | V0.2 整体验收 | `V0.2-整体验收结论.md`，参照 Day6/V0.1 验收报告的证据标准 | 前置阶段0-4均有明确结论（含"不批准/不纳入"这类明确结论，不能是空白） | （汇总五项条款的主状态，不单独定义） | 延期，待前置阶段完成 |

---

## 实验成本、调用次数与运行环境记录要求（V0.2统一要求）

任何涉及 Codex/gpt-image-2 实际调用的任务（阶段2实测、阶段3执行、阶段4实测）必须记录：

- 单次任务的调用次数上限（预算），以及实际使用次数
- 每次调用的时间戳、耗时
- 运行环境：Codex CLI 版本、模型版本（如 gpt-image-2）、执行设备（本地机器，不在此仓库/远程会话中执行）
- 失败次数及失败原因分类（网络错误 / 模型输出不合规 / 其他）
- 失败调用是否计入预算（默认计入，除非任务卡另有说明）

这一要求同步写入下方任务卡集合的"成本上限"和"停止条件"栏，具体记录表格见独立文件《V0.2-成本与运行环境记录模板.md》，不单独重复定义表格结构。**各任务卡的预算相互独立，不得以"沿用其他任务卡/其他阶段剩余额度"为由跳过明确预算审批。**

---

## 与 V0.1 一致的原则（继续遵守）

- 真实人物照片及衍生生成结果仅本地保存，不进入公开 GitHub 仓库
- 文字不交给图像模型生成
- 装饰元素程序化叠加，不由AI每次生成
- 验收结论必须基于实测数据，不允许"部分通过"包装成"通过"
- 生成成本/次数需要有预算上限和记录，不无限制调用

---

## 配套文件索引

- 《阶段0-V0.2首轮实验记录核对清单.md》
- 《V0.2-智能抠图测试设计表.md》
- 《V0.2-浓郁档测量规范.md》
- 《V0.2-回归测试矩阵与授权记录模板.md》
- 《V0.2-接口兼容性审查报告.md》
- 《V0.2-Codex任务卡集合.md》
- 《V0.2-整体验收结论模板.md》（任务卡6使用，当前为空白模板）
- 《V0.2-成本与运行环境记录模板.md》（任务卡2-5涉及实际生成调用时使用，各任务卡预算独立）
- 《V0.2-智能抠图评价工具方案.md》（对应独立工具目录 `scripts/matting-evaluation/`，I/O部分需完成最小样例验证后才能作为任务卡3正式评价工具，见任务卡3前置条件）
- 《V0.2-素材蒙版与授权检查规范.md》（素材编号、蒙版命名与版本、授权枚举、`.gitignore`/`git status`检查项、本地目录规范的单一来源）
- 《V0.2-错误分类与错误映射.md》（V0.2范围内失败原因分类的唯一权威来源，8类，测试表/回归矩阵/任务卡/验收模板均引用此文件，不再各自维护独立分类）
- 《V0.2-V0.1兼容性检查报告.md》（已实际执行`pnpm test`/`pnpm typecheck`确认V0.1基线健康，发现`.gitignore`缺口并已修复）
- 《V0.2-未来接口草案.md》（非冻结，不接入代码，正式用户界面尚未进入当前开发范围）
